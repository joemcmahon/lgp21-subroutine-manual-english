\section{Floating-Point Interpretive Systems}
\label{sec:floating_point_interpreters}

\subsection*{FLOATING POINT INTERPRETER 1 --- H1-11.0}
\addcontentsline{toc}{subsection}{FLOATING POINT INTERPRETER 1 --- H1-11.0}
\label{H1-11.0}
\label{subsec:floating_point_1}

\subsubsection*{PURPOSE}
Floating-Point Interpretive System 1 is a 33-instruction interpreter that lets you write floating-point code on the LGP-21 without managing scale factors by hand. Inputs and outputs are 7-digit decimal mantissas with a separate power-of-10 exponent; the interpreter handles the binary mantissa/exponent split internally.

Each number is packed into one machine word in memory. While the interpreter is operating on a number it unpacks it into two words --- one for the mantissa, one for the exponent --- so the hardware can do ordinary signed arithmetic on each half. The exact bit layout is given under ``Internal Number Format'' below.

The interpreter is bundled with transcendental subroutines (data input/output, sin/cos, arctan, log, exp, sqrt) that are called as interpreter instructions, not as \texttt{B}/\texttt{R}/\texttt{U} calls --- once you're inside the interpreter loop, those operations are part of the instruction set. The interpreter engine itself and the companion fixed-point/floating-point conversion routine (\texttt{I3-12.0}) are entered with a normal machine calling sequence.

\subsubsection*{INTERNAL NUMBER FORMAT}
Each floating-point number is packed into a single word. The word holds two independent signed numbers: a mantissa $M$ and an exponent $E$, representing the value
\[ Z = M \cdot 2^E \]
The mantissa is a sign bit followed by 24 bits of magnitude, treated as a fraction at $q=0$. Mantissas are always stored normalized, so the leading magnitude bit has the place value $2^{-1}$. The exponent is a sign bit followed by 5 bits of magnitude, treated as an integer at $q=30$. Negative values in either field are stored in two's complement of the magnitude bits, with the two fields complemented independently --- so a negative mantissa and a positive exponent can sit in the same word without interfering.

That accounts for 31 of the LGP-21's 32 bits per word; the remaining bit is unused.

\paragraph{Translator's note: discrepancy between prose and worked examples}
The three worked examples below are transcribed from the original manual, but the bit patterns don't quite line up with the field layout described above. The $+3.75$ pattern has the bit pattern \texttt{011111} at the top of the mantissa half, which works out to 0.96875 rather than 0.9375 if you read all 24 magnitude bits as a fraction. The $+0.46875$ pattern has the same issue. The $-3.75$ pattern's mantissa bits don't round-trip to 0.9375 under either one's or two's complement.

It's possible that there's an implicit shift bit between the sign and the mantissa magnitude, or that the diagram bit patterns themselves contain transcription errors. Without a working LGP-21 (or a reference manual that specifies the exact bit fields) it's not safe to commit to either reading. I've left the worked examples as the original presented them; if you have hardware to verify against, please file a PR.

\medskip
\textbf{3.75}:
\[ \texttt{01111100000000000000000000000100} \]
\begin{itemize}[label=---, leftmargin=1.5em, itemsep=2pt]
    \item Mantissa ($q = 0$): $M = 0.9375$
    \item Exponent ($q = 30$): $E = 2$
    \item Verification: $Z = 0.9375 \cdot 2^2 = 3.7500$
\end{itemize}

\medskip
\textbf{-3.75}:
\[ \texttt{10001000000000000000000000000100} \]
\begin{itemize}[label=---, leftmargin=1.5em, itemsep=2pt]
    \item Mantissa ($q = 0$): $M = -0.9375$
    \item Exponent ($q = 30$): $E = 2$
    \item Verification: $Z = -0.9375 \cdot 2^2 = -3.7500$
\end{itemize}

\medskip
\textbf{0.46875}:
\[ \texttt{01111100000000000000000011111110} \]
\begin{itemize}[label=---, leftmargin=1.5em, itemsep=2pt]
    \item Mantissa ($q = 0$): $M = 0.9375$
    \item Exponent ($q = 30$): $E = -1$
    \item Verification: $Z = 0.9375 \cdot 2^{-1} = 0.46875$
\end{itemize}

\medskip
When a value is loaded into the floating-point accumulator or the multiplication register, it expands to two words --- one for the mantissa, one for the exponent --- with the mantissa shifted right by one bit (the ``implicit shift'' mentioned in the translator's note above; it puts the leading magnitude bit at $2^{-2}$ instead of $2^{-1}$). The mantissa and exponent are still paired as one logical value.

\subsubsection*{INTERNAL REGISTERS}
The interpreter provides four registers: a floating-point accumulator, a multiplication register, an address register, and an instruction counter. The accumulator and multiplication register each occupy two words (mantissa + exponent); the address and instruction-counter registers occupy one word each.

All four registers are zero when the program is first loaded. Apart from the instruction counter (which is reset on every entry into the interpreter and incremented per instruction), the registers persist across exits and re-entries: you can leave the interpreter, run native code, and come back without losing register state.

\paragraph{1. Floating-Point Accumulator.} Holds intermediate calculation results. Mantissa at $q=0$ (shifted right by one bit as described above), exponent at $q=30$, each with its own sign. Modified by arithmetic operations, function evaluations, and the Clear, Swap, and Set Sign instructions.

\paragraph{2. Multiplication Register ($M$-register).} Holds the multiplicand for the Multiply and Cumulative Multiplication instructions. Same internal layout as the accumulator. Modified only by the Set and Swap instructions.

\paragraph{3. Address Register.} A single word holding a track/sector value at $q=29$. Used by the Store Address instruction to patch the operand field of an instruction in memory, and by the Test for Zero branch. Modified only by Set Address and Increment Address.

\paragraph{4. Instruction Counter.} Holds the address of the currently-executing interpreter instruction, and points at the next instruction to fetch. Initialized by the main program's calling sequence on entry, and incremented by one per instruction. Jump instructions overwrite it with a new target; the Return instruction stores its current value plus 2 into the operand field of its target location.

\subsubsection*{FLOATING-POINT INSTRUCTIONS}
This interpretive system operates using 33 pseudo-instructions. Each instruction consists of an operation code field and an operand address field. Certain operand addresses carry a divergent structural meaning compared to native fixed-point machine commands (e.g., null/zero addresses).

\noindent
The floating-point instructions are detailed below. The placeholder notation \texttt{ttss} denotes an operand address written in decimal track and sector format. All referenced registers represent the simulated virtual registers of the interpretive engine. All instructions execute to completion without exiting the interpretive system.
\begin{enumerate}
	\item Arithmetic Instructions
\begin{description}[leftmargin=1.5em, itemsep=4pt]
    \item[\texttt{B ttss} (Bring):] Copies the contents of location \texttt{ttss} into the floating-point accumulator. The source memory remains unchanged.
    \item[\texttt{A ttss} (Add):] Adds the contents of location \texttt{ttss} to the current value of the accumulator. The sum replaces the contents of the accumulator. Memory remains unchanged.
    \item[\texttt{S ttss} (Subtract):] Subtracts the contents of location \texttt{ttss} from the current value of the accumulator. The difference replaces the contents of the accumulator. Memory remains unchanged.
    \item[\texttt{D ttss} (Divide):] Divides the current value of the accumulator by the contents of location \texttt{ttss}. The resulting quotient replaces the contents of the accumulator. Memory remains unchanged.
    \item[\texttt{P ttss} (Set M-Register):] Copies the contents of location \texttt{ttss} into the multiplication ($M$) register. Memory remains unchanged.
    \item[\texttt{M ttss} (Multiply):] Multiplies the contents of the $M$-register by the contents of location \texttt{ttss}. The product replaces the contents of the accumulator. The source memory and the $M$-register remain unchanged.
    \item[\texttt{N ttss} (Cumulative Multiplication):] Multiplies the contents of the $M$-register by the contents of location \texttt{ttss}, and automatically adds the product directly to the current accumulator value. The final sum resides in the accumulator. Memory and the $M$-register remain unchanged.
    \item[\texttt{D 000y} (Shift Right):] Divides the current contents of the accumulator by $2^y$. The operand address must be formatted as \texttt{000y}, where $0 \le y \le 9$. The accumulator contents remain strictly in floating-point format.
    \item[\texttt{M 000y} (Shift Left):] Multiplies the current contents of the accumulator by $2^y$. The operand address must be formatted as \texttt{000y}, where $0 \le y \le 9$. The accumulator contents remain strictly in floating-point format.
    \item[\texttt{H ttss} (Hold):] Copies the current contents of the accumulator into location \texttt{ttss}. The accumulator contents remain unchanged.
    \item[\texttt{C ttss} (Clear):] Copies the current contents of the accumulator into location \texttt{ttss}, and subsequently flushes the accumulator register entirely with zeros.
\end{description}

\item Jump / Branch Instructions
\begin{description}[leftmargin=1.5em, itemsep=4pt]
    \item[\texttt{U ttss} (Unconditional Jump):] Overwrites the instruction counter register with the address \texttt{ttss}. Execution loops to fetch the next instruction from location \texttt{ttss}. This command cannot be used to exit the interpretive system.
    \item[\texttt{T ttss} (Test for Minus):] Overwrites the instruction counter register with address \texttt{ttss} if the current accumulator value is negative; execution then branches to \texttt{ttss}. If the accumulator is positive or zero, the branch is ignored and the next sequential instruction is executed. This command cannot be used to exit the interpretive system.
    \item[\texttt{800T ttss} (Conditional Jump):] Overwrites the instruction counter register with address \texttt{ttss} if the accumulator value is negative OR if Program Selector Switch 7 (\texttt{PST}) is turned ON. Otherwise, execution continues sequentially. This command cannot be used to exit the interpretive system.
\end{description}

\item Address Modification Instructions
\begin{description}[leftmargin=1.5em, itemsep=4pt]
    \item[\texttt{E ttss} (Set Address):] Extracts the operand address portion of the instruction word stored at location \texttt{ttss} and copies it into the simulated address register. Memory remains unchanged.
    \item[\texttt{I ttss} (Increment Address):] Increments the current numeric value of the simulated address register by the value \texttt{ttss}. This instruction can be used to decrement the register by supplying the complement of the value \texttt{ttss}.
    \item[\texttt{Y ttss} (Store Address):] Copies the tracking address currently held in the simulated address register into the operand address field of the instruction word at location \texttt{ttss}. The remaining fields of location \texttt{ttss} and the address register itself are unaltered.
    \item[\texttt{Z ttss} (Test for Zero):] Subtracts the literal value \texttt{ttss} from the current contents of the simulated address register. If the result is non-zero, the immediately following sequential instruction is executed. If the result is exactly zero, the next instruction is skipped and execution jumps directly to the second following word (the instruction after next).
    \item[\texttt{R ttss} (Return):] Computes the current value of the instruction counter register plus 2, and writes that calculated address into the operand address field of the instruction word at location \texttt{ttss}. The instruction counter itself and the remaining fields of location \texttt{ttss} are untouched.
\end{description}

\item Auxiliary Instructions

The operand address field for all auxiliary commands must be exactly zero (\texttt{0000}).
\begin{description}[leftmargin=1.5em, itemsep=4pt]
    \item[\texttt{U 0000} (Swap):] Swaps the contents of the multiplication ($M$) register and the floating-point accumulator.
    \item[\texttt{B 0000} (Set Plus Sign):] Forces the accumulator value to be positive if it is currently negative. If the accumulator is already positive or zero, the command is skipped as a no-operation.
    \item[\texttt{T 0000} (Set Minus Sign):] Forces the accumulator value to be negative if it is currently positive. If the accumulator is already negative, the command is skipped as a no-operation.
    \item[\texttt{Y 0000} (Invert Sign):] Replaces the accumulator contents with its arithmetic complement (negating the current sign).
    \item[\texttt{Z 0000} (Stop):] Halts all computation loops immediately, provided that physical Break Point Switch 16 (\texttt{PS-16}) is turned OFF. Pressing the physical ``Start'' button on the console resumes execution at the next sequential location. If \texttt{PS-16} is turned ON, this stop command is bypassed entirely.
    \item[\texttt{E 0000} (Exit):] Terminates execution within the interpretive engine. The instruction immediately following this command word is decoded and executed as a standard native machine instruction.
\end{description}

\item Input / Output Instructions

The operand address field for all I/O commands must be exactly zero (\texttt{0000}).
\begin{description}[leftmargin=1.5em, itemsep=4pt]
    \item[\texttt{I 0000} (Input):] Reads a block of data from tape, automatically parses it into floating-point format, and stores it in sequential memory addresses for main-program access. The target destination base address is read directly from the data tape leader header.
    \item[\texttt{P 0000} (Output):] Prints the current contents of the floating-point accumulator to the output unit in standard floating-point notation. The accumulator state is completely preserved during the print operation.
\end{description}

\item Transcendental Function Evaluation Instructions

The operand address field for all function evaluations must be exactly zero (\texttt{0000}).
\begin{description}[leftmargin=1.5em, itemsep=4pt]
    \item[\texttt{R 0000} (Square Root):] Computes the square root ($\sqrt{x}$) of the current accumulator contents. The result replaces the value in the accumulator.
    \item[\texttt{S 0000} (Sine):] Calculates the sine ($\sin x$) of the current accumulator contents. The result replaces the value in the accumulator. The input argument $x$ must be supplied strictly in radians.
    \item[\texttt{C 0000} (Cosine):] Calculates the cosine ($\cos x$) of the current accumulator contents. The result replaces the value in the accumulator. The input argument $x$ must be supplied strictly in radians.
    \item[\texttt{A 0000} (Arctangent):] Calculates the principal value of the arctangent ($\arctan x$) of the current accumulator contents. The resulting angle is returned strictly in radians.
    \item[\texttt{N 0000} (Natural Logarithm):] Calculates the natural logarithm ($\ln x$, base $x$ relative to $e$) of the current accumulator contents.
    \item[\texttt{H 0000} (Exponential Function):] Computes the natural exponential value ($e^x$), where $x$ represents the initial numeric value held in the accumulator.
\end{description}
\end{enumerate}

\subsubsection*{Data Input and Output 1}
\label{subsec:data_io_1}

\subsubsection*{Data Input}
The ``Data Input and Output 1'' subroutine introduces data into the computer for utilization by a main program. Decimal numbers are read from tape, automatically converted into binary floating-point format, and stored into the specified locations in the required configuration.

This subroutine is invoked directly via an \texttt{I 0000} pseudo-instruction processed within the interpretive loop of the main program. Control automatically returns to the location immediately following this input command once all data elements have been successfully processed and stored.

The input data tape must contain the following expressions in the exact order and format described below:
\begin{enumerate}[label=\alph*)]
\item Input Keyword (Control Word)
The first expression in each distinct group of data elements must be a control word that specifies both the number of decimal places $P$ for every word within that group, and the base starting address \texttt{ttss} of the destination memory block. 

The parameter $P$ must be supplied as a two-digit decimal integer preceded by an algebraic sign, constrained strictly within the range:
\[ -03 \le P \le +16 \]
The starting destination address must be written as an absolute address in decimal track and sector notation (\texttt{ttss}). A positive $P$ value indicates that the decimal point is shifted to the left; a negative $P$ value indicates that the decimal point is shifted to the right.

\item Numeric Data Elements
The individual data entries are represented on tape as signed decimal integers containing up to 7 digits of precision. For positive values, both the explicit plus sign and any leading zeros may be omitted entirely. For negative values, however, all leading zeros between the minus sign and the first significant digit must be explicitly punched on the tape. A value of zero requires only a single stop code (\texttt{'}) to be written.

\medskip
\textbf{Example:} Given an input control keyword punched as \texttt{+032400'}, the fractional decimal value \textbf{16.192} would be encoded and supplied on tape simply as \texttt{16192'}.\footnote{\textbf{Translator's Note:} Because $P = +03$, the interpreter shifts the implied decimal point 3 places to the left upon ingestion, reconstructing $16192 \times 10^{-3} = 16.192$.}

\item End-of-Block Keyword and Minus-Zero Termination
The end of a specific data group is flagged on tape by an explicit minus-zero control word, formatted as \texttt{-0000000'}. This tracking word functions strictly as a structural boundary indicator and is not saved in memory by the subroutine.

The definitive end of the entire reading operation is signaled by appending a second consecutive stop code immediately following this minus-zero control word (i.e., formatted as \texttt{-0000000''}).

When the interpreter reads the singular minus-zero word (\texttt{-0000000'}), the program automatically transitions to read the next sequential input control keyword on the tape, subsequently converting all following data values according to the newly defined $P$ scaling factor and storing them starting at the new destination base address. 

When the double-stop code for the absolute end of the print/read sequence (\texttt{-0000000''}) is encountered instead, the subroutine executes a carriage return and returns execution control back to the main program.

The following example illustrates the physical layout of a typical structured data block:
\begin{table}[h]
\centering
\setlength{\extrarowheight}{4pt} % Slightly increased to maximize the floating effect
\begin{tabular}{|c|c|c|c|c|l|c|c|}
\hline
\multicolumn{3}{|c|}{Input} & S & \multicolumn{2}{c|}{Decimal} & S & \\
\multicolumn{3}{|c|}{control} & t & \multicolumn{2}{c|}{numbers with} & t & \\
\multicolumn{3}{|c|}{word} & o & \multicolumn{2}{c|}{sign} & o & \\ \hline
$\pm$ & P & Location & p & $\pm$ & & p & CR \\ \hline
+ & 07 & 2004 & ' & & 1090000 & ' & \emptybox \\ \hline
  &    &      &   & & \phantom{1}120000 & ' & \xbox \\ \hline
  &    &      &   & & \phantom{1}340000 & ' & \emptybox \\ \hline
  &    &      &   & & \phantom{1}560000 & ' & \xbox \\ \hline
  &    &      &   & & \phantom{1}780000 & ' & \emptybox \\ \hline
  &    &      &   & & \phantom{1}900000 & ' & \xbox \\ \hline
+ &    &      & ' & $-$ & 0000000 & ' & \emptybox \\ \hline
+ & 03 & 2010 & ' & $-$ & 4320001 & ' & \xbox \\ \hline
  &    &      &   & $-$ & 0000021 & ' & \emptybox \\ \hline
  &    &      &   & $-$ & \phantom{000}3470 & ' & \xbox \\ \hline
  &    &      &   & $-$ & \phantom{00000000} & ' & \emptybox \\ \hline
  &    &      &   & $ $ & \phantom{00000}10 & ' & \xbox \\ \hline
  &    &      &   & $-$ & 0000000 & ' & \emptybox \\ \hline
  &    &      &   & $ $ &  & ' & \xbox \\ \hline
\end{tabular}
\caption{LGP-21 Floating Point Tape Input Example}
\end{table}

The first six decimal numbers are read in and stored as follows:

\medskip
\noindent
\begin{tabular}{ll}
\toprule
\textbf{Memory Location} & \textbf{Contents} \\
\midrule
\texttt{2004}        & $109$ \\
\texttt{2005}        & $0.012$ \\
\texttt{2006}        & $0.034$ \\
\texttt{2007}        & $0.056$ \\
\texttt{2008}        & $0.078$ \\
\texttt{2009}        & $0.09$ \\
\bottomrule
\end{tabular}

\medskip
The second record group consisting of five data words would be stored as follows:

\medskip
\begin{tabular}{ll}
\toprule
\textbf{Memory Location} & \textbf{Contents} \\
\midrule
\texttt{2010}        & $-4320.001$ \\
\texttt{2011}        & $-0.021$ \\
\texttt{2012}        & $3.47$ \\
\texttt{2013}        & $0$ \\
\texttt{2014}        & $0.01$ \\
\bottomrule
\end{tabular}

\medskip
The minus-zero control word \texttt{-0000000'} terminates each individual group and triggers the system to read the next record block. All mantissas are maintained at a binary scaling factor of $q = 0$ with a precision length of 24 bits. All exponents are stored within the exact same memory word as their respective mantissa, scaled at $q = 30$. The additional tracking stop code appended behind the second minus-zero control word completely terminates the data input sequence.
\end{enumerate}

\subsubsection*{2. Data Output}
The ``Data Input and Output 1'' subroutine delivers the contents of the floating-point accumulator as a signed, seven-digit decimal mantissa and a signed, two-digit decimal exponent. Following the output execution, a tabulator character (tab space) is issued, and control branches back to the location immediately following the \texttt{P 0000} instruction that originally invoked the subroutine.

\paragraph{Output Format}
The printed output expression uses the following structural form:
\[ \texttt{.XXXXXXX$\pm$\ EE$\pm$} \]

Where:
\begin{itemize}[label=---, leftmargin=1.5em, itemsep=2pt]
    \item \texttt{.XXXXXXX$\pm$} represents the decimal mantissa with its trailing sign.
    \item \texttt{EE$\pm$} represents the power-of-10 base exponent with its trailing sign.
\end{itemize}

\paragraph{Output Examples}
The following table illustrates the raw machine printout format paired with its conventional decimal representation:

\medskip
\begin{tabular}{ll}
\toprule
\textbf{Machine Printout} & \textbf{Conventional Representation} \\
\midrule
\texttt{.5060000-\ 04\ }  & $-5060.000$ \\
\texttt{.0937500-\ 02-}  & $-0.000937500$ \\
\texttt{.1000000-\ 06-}  & $-0.0000001000000$ \\
\texttt{.1000000-\ 10\ }  & $-0.1 \times 10^{-10}$ \\
\bottomrule
\end{tabular}

\subsubsection*{SINE-COSINE 3 --- B1-10.2}
``Sine-Cosine 3'' calculates the sine or cosine of an argument provided in floating-point radians. Upon returning to the main program, the result is stored in the floating-point accumulator. The return is executed to the location immediately following the \texttt{S0000} or \texttt{C0000} instruction, depending on which subroutine was invoked.

\subsubsection*{ARCTANGENT 2 --- B1-11.2}
``Arctangent 2'' calculates the arctangent of an argument located in the floating-point accumulator. The result is placed in the floating-point accumulator in radians when control returns to the location of the main program following the \texttt{A0000} instruction.

\subsubsection*{LOGARITHM 2 --- B3-11.1}
``Logarithm 2'' calculates the natural logarithm (base $e$) of a number located in the floating-point accumulator. The result remains in the floating-point accumulator upon return to the location of the main program following the \texttt{N0000} instruction.

\subsubsection*{EXPONENTIAL FUNCTION 2 --- B3-10.1}
``Exponential Function 2'' calculates the value $e^x$, where $x$ is the contents of the floating-point accumulator. The result is stored in the floating-point accumulator upon return to the location of the main program following the \texttt{H0000} instruction.

\subsubsection*{SQUARE ROOT 2 --- B4-10.1}
``Square Root 2'' calculates the square root of a positive number located in the floating-point accumulator. The result is placed in the floating-point accumulator upon return to the location of the main program following the \texttt{R0000} instruction.

\subsubsection*{CALLING SEQUENCE}

The ``Floating-Point Interpreter System 1'' is invoked via a calling sequence from the main program. Prior to branching into the system, the starting address of the floating-point instructions to be interpreted must be supplied to the system. The floating-point instructions must immediately follow the branch instruction. An example is provided below:

\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha$             & \texttt{R}                    & $L_0$            & Store starting address of floating-point instructions. \\
$\alpha+1$           & \texttt{U}                    & $L_0$            & Entry into the interpreter system. \\
$\alpha+2$           & \multicolumn{2}{c}{$\langle$\dots$\rangle$}              & Floating-point instructions. \\
$\alpha+n$           & \multicolumn{2}{c}{$\langle$\dots$\rangle$}              & \\
$\alpha+n+1$         & \texttt{XE}                   & \texttt{0000}             & Exit from the interpreter system. Return to fixed-point instructions. \\
\bottomrule
\end{tabularx}
\end{table}

Locations $\alpha$ and $\alpha+1$ contain instructions \texttt{R} and \texttt{U}, which store the target address ($\alpha+2$) into the interpreter system and execute a branch to $L$, the starting address of the interpreter system. Locations $\alpha+2$ through $\alpha+n$ contain the actual floating-point instructions. An explicit exit from the interpreter system is required when, as shown here at $\alpha+n+1$, fixed-point machine instructions follow.

\subsection*{FIXED-POINT TO FLOATING-POINT CONVERSION (AND VICE VERSA) --- L3-12.0}
\addcontentsline{toc}{subsection}{FIXED-POINT TO FLOATING-POINT CONVERSION (AND VICE VERSA) --- L3-12.0}

This program serves two functions. It converts a fixed-point number into the floating-point format defined for ``Floating-Point Interpreter System 1'', or it converts a floating-point number back into a fixed-point number. This program is not an integral part of the interpreter system core; rather, it is a fixed-point subroutine designed to convert data intended for, or generated by, the interpreter system.

Before executing the calling sequence for this subroutine, the interpreter system must be exited. The number to be converted must reside in the hardware accumulator, and its scale factor $q$ must be supplied by the calling sequence.

When converting back, a floating-point number must not be too large to be held at the specified $q$. When converting forward, a fixed-point number must not require an exponent greater than 31.

Upon return to the main program, the converted number resides in the machine accumulator. The value will either be in fixed-point form at the $q$ provided by the calling sequence (if converted back), or in floating-point form corresponding to the specified $q$ (if converted forward).

The respective calling sequences are explained below. The first is for converting a number from fixed-point to floating-point, and the second is for converting a number from floating-point back to fixed-point. Only one operation per number can be executed during a single call.

\subsubsection*{Conversion to floating-point}
\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-1$               & \texttt{B}                    & $\langle \dots \rangle$              & Load fixed-point number into accumulator. \\
$\alpha$                 & \texttt{R}                    & $L+25$             & Store address of $q$. \\
$\alpha+1$               & \texttt{U}                    & $L$                & Entry into the subroutine. \\
$\alpha+2$               & \texttt{Z}                    & $089q$           & Specify $q$ of the number. \\
$\alpha+3$               & \multicolumn{2}{c}{etc.}               & Return to main program. \\
\bottomrule
\end{tabularx}
\end{table}

The instruction at $\alpha-1$ loads the fixed-point number into the accumulator (any instruction achieving this may be used). The instruction at $\alpha$ stores the address ($\alpha+2$) of the value $q$ into the subroutine. The instruction at $\alpha+1$ causes a branch to $L$, the starting address of this subroutine. The instruction at $\alpha+2$ contains the $q$ of the fixed-point number formatted as a decimal sector address. The return from the subroutine occurs at $\alpha+3$.

\subsubsection*{Conversion to fixed-point}

\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-1$               & \texttt{B}                    & $\langle \dots \rangle$              & Load floating-point number into accumulator. \\
$\alpha$                 & \texttt{R}                    & $L+148$            & Store address of $q$. \\
$\alpha+1$               & \texttt{U}                    & $L+122$            & Entry into the subroutine. \\
$\alpha+2$               & \texttt{Z}                    & $08qq$           & Specify target $q$ for the number. \\
$\alpha+3$               & \multicolumn{2}{c}{etc.}               & Return to main program. \\
\bottomrule
\end{tabularx}
\end{table}

The instruction at $\alpha-1$ loads the floating-point number into the primary hardware accumulator (any instruction achieving this is permissible). The instruction at $\alpha$ stores the address ($\alpha+2$) of the value $q$ into the subroutine. The instruction at $\alpha+1$ causes a branch to the appropriate entry location, $L+122$, of this subroutine. The instruction at $\alpha+2$ contains the $q$ that the converted fixed-point number should assume, formatted as a decimal sector address. The return from the subroutine occurs at $\alpha+3$.

\subsubsection*{PROGRAM STOPS}

There are two dedicated program stops within this subroutine. They are listed and explained below. $L$ represents the starting address of this subroutine.

\begin{table}[h!]
\centering
\setlength{\extrarowheight}{5pt}
\begin{tabular}{|c|p{8.5cm}|}
\hline
\textbf{Memory Location} & \textbf{Meaning and Remedy} \\ \hline
$L+152$ & The number is too large for the specified $q$ during backward conversion. Pressing ``Start'' clears the accumulator and returns control to the main program. \\ \hline
$L+262$ & The number requires an exponent greater than 31 during forward conversion. Pressing ``Start'' clears the accumulator and returns control to the main program. \\ \hline
\end{tabular}
\end{table}

Note that this subroutine is utilized independently. An input or output unit is not required. Storage requirements are detailed below.

\subsubsection*{STORAGE REQUIREMENTS}
Only ``Square Root 2'' is natively bundled inside the actual interpreter system core. The other subroutines are provided individually. They can be loaded partially or entirely depending on application needs. The subroutines are listed below alongside their relative starting addresses and required memory footprints (where $L$ represents the starting address of the interpreter system):

\begin{table}[h]
\centering
\setlength{\extrarowheight}{3pt}
\begin{tabular}{|l|c|c|l|}
\hline
\textbf{Subroutine} & \textbf{Initial Filler} & \textbf{Modifier} & \textbf{Memory Required} \\ 
                    & \textbf{Keyword}        & \textbf{Keyword}  &                          \\ \hline
Interpreter System  & L                       & L                 & 10 Tracks \\ \hline
Data Input          & L+1000                  & L+1000            & 7 Tracks  \\ 
\hfil \& Output 1   &                         &                   &           \\ \hline
Sine-Cosine 3       & L+1700                  & L+1700            & 2 Tracks, 32 Sectors \\ \hline
Arctangent 2        & L+1932                  & L+1932            & 1 Track, 32 Sectors  \\ \hline
Logarithm 2         & L+2100                  & L+2100            & 1 Track              \\ \hline
Exponential Function 2 & L+2200               & L+2200            & 2 Tracks             \\ \hline
Fixed/Floating Conversion & \textit{Anywhere} & \textit{Anywhere} & 3 Tracks             \\ 
\hfil \& Vice Versa 1     &                   &                   &                      \\ \hline
\end{tabular}
\end{table}

Track 63 is utilized by various parts of the system as an intermediate scratchpad memory, with the sole exception of ``Fixed-Point to Floating-Point Conversion and Vice Versa 1'', which requires no scratchpad storage. Therefore, no part of the system should ever be stored in Track 63.

\subsubsection*{OPERATION}

\paragraph{1. Program Input}
The ``Floating-Point Interpreter System 1'' is provided on two separate paper tapes. The first tape contains the program in an arbitrarily relocatable hexadecimal format, compatible with Program Input 1 (\texttt{J1-10.0}). The second tape contains the program in an arbitrarily relocatable decimal format structured in coding sheet layout. Each tape contains the interpreter system core followed by all subroutines in the exact order specified under ``Storage Requirements''. It is assumed that Program Input 1 starts at address \texttt{0000}.

If program selector switch \textsf{PS-32} is turned \textsf{ON}, the relocatable hexadecimal program tape can be read continuously without manual intervention until the ``Fixed-Point to Floating-Point Conversion and Vice Versa'' routine is reached. If \textsf{PS-32} is turned \textsf{OFF}, the computer will halt after each successfully read subroutine. (No stop is hardcoded between the Interpreter System core and ``Data Input and Output 1''.) 

In either case, entering a new modifier keyword and a select keyword for the input device is strictly required before loading ``Fixed-Point to Floating-Point Conversion and Vice Versa''. This specific program must \emph{never} be loaded using the same modifier as any part of the interpreter system core.

The relocatable decimal tape has a hardcoded stop programmed after the interpreter system core and after every individual subroutine. In all cases when using this tape, a new modifier and a new device selection keyword must be entered manually for each subroutine to be read.

For both tape types, whenever a stop occurs due to \textsf{PS-32 OFF}, the operator must press the \textsf{``Start''} button to return execution to Program Input 1 for subsequent data entry. If a stop occurs while \textsf{PS-32} is \textsf{ON}, the computer will halt directly on an input command within Program Input 1.

\paragraph{2. Required Input/Output Units}
The Flexowriter must be selected for both input and output operations.

\paragraph{3. Program Selector Switches}
\begin{table}[h]
\centering
\setlength{\extrarowheight}{3pt}
\begin{tabular}{|l|l|p{8.5cm}|}
\hline
\textbf{Switch} & \textbf{Setting} & \textbf{Function} \\ \hline
\textsf{PS-16}  & \textsf{ON}               & Suppresses the programmatic halt at a \texttt{Z 0000} instruction. \\ 
                & \textsf{OFF}              & Permits a programmatic halt at a \texttt{Z 0000} instruction. \\ \hline
\textsf{PS-32}  & \textsf{ON}               & Forces an unconditional branch upon encountering an \texttt{-T} instruction\footnote{The \texttt{-T} instruction is a special case branch-on-negative test that branches unconditionally if \textsf{PS-32} is \textsf{ON}.} \\ 
                & \textsf{OFF}              & Forces a conditional branch upon encountering an \texttt{-T} instruction. \\ \hline
\end{tabular}
\end{table}

\paragraph{4. Program Stops}
$L$ represents the absolute starting address of the interpreter system.

\begin{table}[h]
\centering
\setlength{\extrarowheight}{4pt}
\begin{tabular}{|c|p{5cm}|p{4.5cm}|}
\hline
\textbf{Location} & \textbf{Meaning} & \textbf{Remedy} \\ \hline
$L+557$ & The exponent is too large for an \texttt{H} or \texttt{C} instruction, or the argument of a square root is negative. The address of the offending instruction resides in the machine accumulator. & Press ``Start'' to bypass. The \texttt{H} instruction, \texttt{C} instruction, or square root calculation will not be executed. \\ \hline
$L+759$ & Division by an illegal value (Division by Zero). & Do not proceed. The data value must be corrected. \\ \hline
$L+1152$ & The input value possesses an exponent that is too large. & Press ``Start'' to advance to the next pseudo-instruction. A value of zero is stored in place of the rejected number. \\ \hline
$L+2005$ & The argument for a natural logarithm is $\le 0$. & Press ``Start'' to proceed with a computed result of zero. \\ \hline
$L+2056$ & The exponent is too large for a natural logarithm operation. & Do not proceed. The data value must be corrected. \\ \hline
\end{tabular}
\end{table}

\subsubsection*{REMARKS}

The initial location of the interpreter system should ideally align with Sector \texttt{00} of a track so that the reference addresses targeting Track 63 are optimally mapped.

Remember that when the interpreter is running all instructions are pseudo-instructions! The interpreter is substituting itself for the standard fetch-execute cycle that the CPU would do. Another way of saying it is that the interpreter is an emulator for a CPU that has a floating-point instruction set, and we have an agreed-upon protocol to enter and exit emulation.

Pseudo-instructions utilizing addresses from \texttt{0000} to \texttt{0009} have special meanings. When pseudo-instructions use these addresses, they do not refer to the actual storage at these addresses, but instead invoke functions in the interpreter. The interpreter utilizes 16 such special instructions, including \texttt{D 000y} and \texttt{M 000y}. Although instructions \texttt{D 000y} and \texttt{M 000y} may carry a literal value of $y$ between 1 and 9, standard instructions like \texttt{D ttss} and \texttt{M ttss} (while in interpreter mode) cannot reference any location between \texttt{0000} and \texttt{0009}\footnote{The addresses from \texttt{0000} to \texttt{0009}, inclusive, trigger transcendental functions, etc. \emph{when the floating-point interpreter is running}. In normal execution mode, they're used just the same as any other location in the machine.}.

\subsubsection*{EXAMPLE}

This example demonstrates how to evaluate a fourth-degree polynomial using the Floating-Point Interpreter System's extended instruction set. The polynomial is structured as:
\[ y = Ax^4 + Bx^3 + Cx^2 + Dx + E \]

By applying Horner's Method, the equation is factored to minimize arithmetic operations, reducing it to a sequence of successive multiplications and additions:
\[ y = (((Ax + B)x + C)x + D)x + E \]

The Interpreter System core is loaded at address \texttt{0300}. The main program begins at location \texttt{4200}. The five floating-point coefficients ($A, B, C, D, E$) and the variable $x$ are stored as 3-word blocks starting at location \texttt{4306}.

\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabular}{llll}
\toprule
\textbf{Loc} & \textbf{Op} & \textbf{Addr} & \textbf{Remarks / Description} \\ 
\midrule
4200 & R  & 0300 & ; Set up return link pointer for interpreter loop \\
4201 & U  & 0300 & ; Enter Interpreter Mode (Jumps to system core) \\
4202 & B  & 4306 & ; Load coefficient A into pseudo-accumulator \\
4203 & M  & 4324 & ; Multiply by x    [Ax] \\
4204 & A  & 4309 & ; Add coefficient B [Ax + B] \\
4205 & M  & 4324 & ; Multiply by x    [(Ax + B)x] \\
4206 & A  & 4312 & ; Add coefficient C [(Ax + B)x + C] \\
4207 & M  & 4324 & ; Multiply by x \\
4208 & A  & 4315 & ; Add coefficient D \\
4209 & M  & 4324 & ; Multiply by x \\
4210 & A  & 4318 & ; Add coefficient E [Final polynomial value y] \\
4211 & H  & 4321 & ; Store calculated result y into memory block \\
4212 & XE & 0000 & ; Exit Interpreter Mode (Return to native hardware) \\
4213 & H  & 0000 & ; Native machine execution halt \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{2. Data Memory Allocation Map}

Because the pseudo-accumulator manages multi-word operations, each floating-point literal spans 3 consecutive words on the magnetic drum. 

\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Start Address} & \textbf{Symbol} & \textbf{Allocation Footprint} \\ 
\midrule
4306 & A & Occupies locations 4306, 4307, 4308 \\
4309 & B & Occupies locations 4309, 4310, 4311 \\
4312 & C & Occupies locations 4312, 4313, 4314 \\
4315 & D & Occupies locations 4315, 4316, 4317 \\
4318 & E & Occupies locations 4318, 4319, 4320 \\
4321 & Y & Result storage (locations 4321, 4322, 4323) \\
4324 & X & Input variable x (locations 4324, 4325, 4326) \\
\bottomrule
\end{tabularx}
\end{table}

\subsubsection*{INSTRUCTION SUMMARY}

\paragraph{Operational Notation Definitions}
\begin{table}[h]
\small
\ttfamily
\begin{tabular}{ll}
( )  & Contents of location or register \\
$\rightarrow$  & Displaces / moves to / becomes \\
Acc & Floating-Point Software Accumulator \\
A Reg. & Address Register \\
M Reg. & Multiplier Register \\
C Reg. & Counter Register \\
\end{tabular}
\end{table}

\paragraph{Extended Interpreter Instruction Set}

\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Op} & \textbf{Operand (ttss)} & \textbf{Result / Action} \\ 
\midrule
B & ttss & (ttss) $\rightarrow$ Acc \\
A & ttss & (Acc) + (ttss) $\rightarrow$ Acc \\
S & ttss & (Acc) - (ttss) $\rightarrow$ Acc \\
D & ttss & (Acc) / (ttss) $\rightarrow$ Acc \\
P & ttss & (ttss) $\rightarrow$ M Reg. \\
M & ttss & (M Reg.) $\times$ (ttss) $\rightarrow$ Acc \\
N & ttss & (M Reg.) $\times$ (ttss) + (Acc) $\rightarrow$ Acc \\
H & ttss & (Acc) $\rightarrow$ ttss \\
C & ttss & (Acc) $\rightarrow$ ttss; then 0 $\rightarrow$ Acc \\
U & ttss & Unconditional Jump: Next instruction is at ttss \\
T & ttss & Conditional Jump: Jump to ttss if (Acc) $<$ 0 \\
-T& ttss & Conditional Jump: Jump to ttss if (Acc) $<$ 0 OR if \textsf{PST} is \textsf{ON} \\
E & --   & Exit Interpreter Mode \\
I & ttss & ttss + (A Reg.) $\rightarrow$ A Reg. \\
Y & ttss & (Address Part of ttss) $\rightarrow$ A Reg. \\
Z & ttss & (A Reg.) $\rightarrow$ Address Part of ttss \\
R & ttss & Compare: If (A Reg.) - ttss = 0, skip next instruction; else loop \\
\bottomrule
\end{tabularx}
\end{table}
\newpage
\paragraph{Special Behavior for Null Addresses}
When the address field \texttt{ttss} of an interpreter command equals \texttt{0000} (or a designated function selector ID within Track 00), the default memory interaction is overridden to perform specialized administrative or algorithmic operations rather than referencing physical location zero.

\paragraph{Interpreter Behavior for Null Addresses (Address = 0000)\protect\footnote{Excluding extended subroutine selection commands under the format \texttt{D000y} or \texttt{M000y}. See Section~0479.}}

\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{Op} & \textbf{Result / Action Executed (When Address Field is 0000)} \\ 
\midrule
D & (Acc)$^2$ $\rightarrow$ Acc \hfill [Square the Accumulator] \\
M & (Acc) $\times$ 2 $\rightarrow$ Acc \hfill [Double the Accumulator] \\
U & Swap values: (Acc) $\longleftrightarrow$ (M Reg.) \\
B & Make (Acc) positive \hfill [Absolute Value: $|Acc|$] \\
T & Make (Acc) negative \hfill [Force Negative Sign] \\
Y & Invert Sign: Change sign bit of (Acc) \\
Z & Conditional Program Stop (Halts execution if console switch \textsf{PS-16} is \textsf{OFF}) \\
E & Exit Interpreter Mode (Return control to native machine hardware) \\
I & Read floating-point numbers from paper tape input \\
P & Print/Punch floating-point numbers to output device \\
R & $\sqrt{\text{(Acc)}}$ $\rightarrow$ Acc \hfill [Square Root] \\
S & Sine(Acc) $\rightarrow$ Acc \\
C & Cosine(Acc) $\rightarrow$ Acc \\
A & Arctangent(Acc) $\rightarrow$ Acc \\
N & $\ln\text{(Acc)}$ $\rightarrow$ Acc \hfill [Natural Logarithm] \\
H & $e^{\text{(Acc)}}$ $\rightarrow$ Acc \hfill [Exponential Function] \\
\bottomrule
\end{tabularx}
\end{table}
\newpage
\subsection*{FLOATING POINT INTERPRETER 2 --- H1-11.1}
\addcontentsline{toc}{subsection}{FLOATING POINT INTERPRETER 2 --- H1-11.1}
\label{H1-11.1}

\subsubsection*{INTRODUCTION}

The ``Floating-Point Interpreter System 2'' permits the programming of LGP-21 problems using floating-point arithmetic. The system accepts 9-digit whole decimal numbers as input data, scaled by a two-digit decimal exponent. It automatically handles all required scaling modifications during calculation and outputs results as a 7-digit signed decimal mantissa, followed by a 2-digit decimal exponent.

Because the system autonomously processes values across an exceptionally wide dynamic range, the programmer does not require precise prior knowledge of a problem's numerical boundaries. This structural capacity guarantees a high degree of calculation and output accuracy. Furthermore, this program expands the standard 16-instruction hardware set of the computer to \textbf{33 instructions}, making operations as simple and user-friendly as possible.

The ``Floating-Point Interpreter System 2'' is available as a single paper tape in an arbitrarily relocatable hexadecimal format, compatible with Program Input 1 (\texttt{J1-10.0}). It bundles various subroutines—such as Data Input, Data Output, and a suite of transcendental math functions—which are detailed later in this manual. Only the specific subroutines required for a given problem need to be read into memory alongside the main ``Floating-Point Interpreter System 2'' core; however, the memory cross-references for the entire system must be strictly preserved during loading.

The programmer will quickly find that utilizing this system yields a massive savings of time and effort when dealing with problems where data values fluctuate wildly in magnitude—scenarios that would otherwise demand an immense, complex programming overhead under traditional fixed-point arithmetic constraints.

\paragraph{Principles}
The ``Floating-Point Interpreter System 2'' intercepts and reinterprets the elementary hardware instruction structure of the LGP-21, transforming it so that it can be programmed exactly like a natively ``hardwired'' floating-point computer. This illusion is achieved through:

\begin{enumerate}
    \item \textbf{Virtual Registers:} The provision of an internal, software-driven Multiplier Register, Address Register, and a dedicated Floating-Point Accumulator.
    \item \textbf{Advanced Operations:} Integrated support for cumulative multiplication (multiply-accumulate), sign inversion, and direct function-generating commands.
    \item \textbf{Massive Numerical Range:} Input data consists of signed 9-digit decimal numbers followed by a base-10 exponent between $+99$ and $-99$. Output data delivers signed 7-digit decimal numbers followed by a base-10 exponent between $+99$ and $-99$.
    \item \textbf{Expanded I/O framework:} A vastly modernized structure for data entry and terminal printing commands.
\end{enumerate}

The entire system footprint consumes \textbf{28 Tracks} of memory. This is divided into the core interpreter loop, Data Input (7 tracks), Data Output (4 tracks), and Floating-Point Functions (6 tracks). This leaves \textbf{36 Tracks (2,304 words)} completely free on the magnetic drum for the programmer's main execution routine and data storage arrays.

\subsubsection*{INTERNAL REGISTERS}

The floating-point format requires two consecutive locations to represent a single data word. Consequently, the interpreter system features two specialized registers designed for data transport, processing, and output while strictly preserving the floating-point structure. These registers—the Floating-Point Accumulator and the Multiplier Register—each occupy two locations. 

Address modification can be executed directly within the interpreter system by utilizing a simulated, single-location Address Register. Additionally, a single-location Counter Register governs the operational execution flow of the interpreter system loop.

Upon initial system loading, before any operations have commenced, the contents of all virtual registers are initialized to zero. A detailed breakdown of these specialized registers follows below. (The individual instructions referenced here are explained comprehensively in Section 4, ``Pseudo-Instructions'').

\paragraph{1. The Floating-Point Accumulator}
The Floating-Point Accumulator holds intermediate mathematical results and output values. Because output data must ultimately be rendered as a signed 7-digit decimal mantissa with a 2-digit decimal exponent, the hardware treats them internally as follows:
\begin{itemize}
	\item \textbf{The Mantissa:} Maintained within the Floating-Point Accumulator with a precision of 30 bits at binary scaling factor $q = 0$. The most significant bit possesses a weight of $2^{-1}$, meaning the mantissa is *always* strictly normalized.
	\item \textbf{The Exponent:} Positioned at binary scaling factor $q = 29$. 
\end{itemize}

Both the mantissa and the exponent maintain their own independent, algebraic sign bits. The contents of the Accumulator are altered exclusively by arithmetic operations, transcendental function calls, or explicit register management commands (Clear, Swap, or Set Sign).

\paragraph{2. The Multiplier Register}
The Multiplier Register stores the multiplicand during standard multiplication and cumulative multiplication (multiply-accumulate) routines. 
* The internal mantissa is mapped at $q = 0$.
* The internal exponent is mapped at $q = 29$.

Like the accumulator, both components carry their respective algebraic sign bits. The contents of this register can only be modified by executing explicit ``Set'' or ``Swap'' commands.

\paragraph{3. The Address Register}
The Address Register holds a single virtual memory address formatted in Track/Sector notation at binary scaling factor $q = 29$. This register is specifically utilized to modify the operand address field of memory words via the ``Store Address'' command, and to govern main program execution loops during zero-testing routines. The contents of this register are altered exclusively by an explicit ``Set Address'' or ``Increment Address'' instruction.

\paragraph{4. The Counter Register}
The Counter Register functions as the virtual Program Counter (PC). It tracks the absolute address of the currently executing pseudo-instruction and points ahead to the address of the subsequent instruction. 

When a subroutine return sequence is initiated, the current contents of this register plus 2 ($\text{PC} + 2$) are injected into the operand address field of the targeted return command. The register is initially primed by the main program's calling sequence and automatically increments by 1 following the execution of each pseudo-instruction. This linear, step-by-step increment can only be overridden by a programmatic branch or jump instruction.

\subsubsection*{INTERNAL NUMBER FORMAT}

Every floating-point number requires two consecutive locations for its complete representation. 

\begin{itemize}
    \item \textbf{The first location} holds the mantissa of the number in binary representation scaled at $q = 0$. Each mantissa consists of an algebraic sign bit and 30 significant binary digits; the most significant bit (MSB) carries a value of $2^{-1}$. The mantissa is therefore always maintained in a strictly normalized state.
    \item \textbf{The second location} holds the base-2 exponent, which must be multiplied by the mantissa to obtain the actual value of the represented number. This exponent, maintained at $q = 29$, is a whole binary integer carrying its own algebraic sign bit.
\end{itemize}

Taken together, these two components represent a floating-point number via the following mathematical relationship:
\[ Z = M \cdot 2^E \]
Where $Z = \text{The Number}$, $M = \text{The Mantissa}$, and $E = \text{The Exponent}$.

\paragraph{Example 1: Representation of $+3.75$}
In floating-point memory, the decimal value $3.75$ is internally normalized as $0.9375 \cdot 2^2$. Its exact binary equivalent on the magnetic drum is structured as follows:

\vspace{1em}
\noindent\textbf{First Location (Mantissa at $q = 0$):}
\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabular}{|c|c|c|}
\hline
\textbf{Bit 0} & \textbf{Bits 1--30} & \textbf{Bit 31} \\ \hline
0 & 111100000000000000000000000000 & 0 \\ \hline
\multicolumn{1}{c}{$\uparrow$} & \multicolumn{1}{c}{$\uparrow$} & \multicolumn{1}{c}{$\uparrow$} \\
\multicolumn{1}{c}{\small Sign (+)} & \multicolumn{1}{c}{\small Normalized Mantissa ($0.9375$)} & \multicolumn{1}{c}{\small Unused} \\
\end{tabular}
\end{table}

\noindent\textbf{Second Location (Exponent at $q = 29$):}
\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabular}{|c|c|c|}
\hline
\textbf{Bit 0} & \textbf{Bits 1--29} & \textbf{Bits 30--31} \\ \hline
0 & 00000000000000000000000000000 & 10 \\ \hline
\multicolumn{1}{c}{$\uparrow$} & \multicolumn{1}{c}{$\uparrow$} & \multicolumn{1}{c}{$\uparrow$} \\
\multicolumn{1}{c}{\small Sign (+)} & \multicolumn{1}{c}{\small Padding Zone} & \multicolumn{1}{c}{\small Binary Exponent ($2$)} \\
\end{tabular}
\end{table}

\paragraph{Example 2: Representation of a Negative Number ($-3.75$)}
A negative number is represented by inverting the leading sign bit of the mantissa location:

\vspace{1em}
\noindent\textbf{First Location (Mantissa at $q = 0$):}
\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabular}{|c|c|c|}
\hline
\textbf{Bit 0} & \textbf{Bits 1--30} & \textbf{Bit 31} \\ \hline
1 & 000100000000000000000000000000 & 0 \\ \hline
\multicolumn{1}{c}{$\uparrow$} & \multicolumn{1}{c}{$\uparrow$} & \multicolumn{1}{c}{$\uparrow$} \\
\multicolumn{1}{c}{\small Sign ($-$)} & \multicolumn{1}{c}{\small Two's Complement Mantissa} & \multicolumn{1}{c}{\small Unused} \\
\end{tabular}
\end{table}
\paragraph{Sequential Allocation in Drum Memory}
The continuous storage of alternating mantissas and exponents across sequential drum tracks steps through memory in fixed increments of two:

\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabular}{ll}
\toprule
\textbf{Memory Location} & \textbf{Location Contents} \\ 
\midrule
4304 & Mantissa of Variable 1 \\
4305 & Binary Exponent of Variable 1 \\
4306 & Mantissa of Variable 2 \\
4307 & Binary Exponent of Variable 2 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Operational Boundaries (Note):}
\begin{enumerate}
    \item \textbf{Input Restriction:} During decimal data entry, the input base-10 exponent is strictly bounded to the range $-99 \le E_{10} \le +99$.
    \item \textbf{Output Restriction:} During printing operations via the paper tape typewriter, the printed base-10 exponent is likewise bounded to $-99 \le E_{10} \le +99$.
    \item \textbf{Binary Underflow/Overflow Guard:} When executing internal calculations or storing the contents of the virtual Floating-Point Accumulator into memory, the computer will \emph{not} halt (i.e., no hardware overflow/underflow fault is triggered), provided that the internal binary exponent satisfies the following safety threshold:
    \[ -333 \le E_{2} \le +333 \]
\end{enumerate}

\subsubsection*{PSEUDO-INSTRUCTIONS}

The following section details the complete catalog of the 33 virtual instructions compiled for the ``Floating-Point Interpreter System 2'' and explains their operational mechanics. Any reference to the ``Accumulator'' designates the designated two-location memory block allocated to the software Floating-Point Accumulator.

\paragraph{Arithmetic Instructions}
\textit{Note: \texttt{XXXX} represents the absolute starting memory address of the target floating-point number's mantissa.}

\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Opcode} & \textbf{Name} & \textbf{Operational Action / Mechanics} \\ 
\midrule
BXXXX & Bring & (XXXX) $\rightarrow$ Accumulator \\
AXXXX & Add & (Accumulator) + (XXXX) $\rightarrow$ Accumulator \\
SXXXX & Subtract & (Accumulator) $-$ (XXXX) $\rightarrow$ Accumulator \\
DXXXX & Divide & (Accumulator) / (XXXX) $\rightarrow$ Accumulator \\
PXXXX & Place & (XXXX) $\rightarrow$ M Register \\
MXXXX & Multiply & (M Register) $\times$ (XXXX) $\rightarrow$ Accumulator \\
NXXXX & Multiply Cumulatively & [(M Register) $\times$ (XXXX)] + (Accumulator) $\rightarrow$ Accumulator \\
XD000y & Divide by $2^y$ & (Accumulator) / $2^y$ $\rightarrow$ Accumulator \hfill ($0 \le y \le 9$) \\
XM000y & Multiply by $2^y$ & (Accumulator) $\times$ $2^y$ $\rightarrow$ Accumulator \hfill ($0 \le y \le 9$) \\
HXXXX & Hold & (Accumulator) $\rightarrow$ XXXX \\
CXXXX & Clear & (Accumulator) $\rightarrow$ XXXX; then $0 \rightarrow$ Accumulator \\
\bottomrule
\end{tabularx}
\end{table}

For the power-of-two scaling commands (\texttt{XD000y} and \texttt{XM000y}), the accumulator strictly maintains its internal normalized floating-point structure throughout the bit-shifting sequence.

\paragraph{Logical or Branch Instructions}
\textit{Note: \texttt{XXXX} represents the target instruction address on the magnetic drum.}

\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Opcode} & \textbf{Name} & \textbf{Operational Action / Mechanics} \\ 
\midrule
UXXXX & Unconditional Jump & Forces execution loop to target location \texttt{XXXX}.\footnote{Crucial constraint: This instruction must not be used to exit the interpreter environment.} \\
TXXXX & Test Minus & Jumps to \texttt{XXXX} if (Accumulator) $< 0$; else executes next sequential line. \\
800TXXXX & Conditional Jump & Jumps to \texttt{XXXX} if (Accumulator) $< 0$ \textbf{OR} if console switch \textsf{PST} is \textsf{ON}. \\
XXXXX & Set Address & (Address Part of XXXX) $\rightarrow$ Address Register \\
IXXXX & Increment Address & (Address Register) + XXXX $\rightarrow$ Address Register\footnote{Can perform subtraction/decrements by supplying the two's complement of the address part in the instruction word.} \\
YXXXX & Store Address & (Address Part of Address Register) $\rightarrow$ Address Part of location XXXX \\
ZXXXX & Zero Test & (Address Register) $-$ \texttt{XXXX}. If result $= 0$, skip next instruction; else execute next line. \\
RXXXX & Return & Links subroutines: ($\text{PC} + 2$) $\rightarrow$ Address Part of target location XXXX. \\
\bottomrule
\end{tabularx}
\end{table}

\paragraph{Auxiliary Instructions}
\textit{Note: The address field (\texttt{XXXX}) for all auxiliary operations must be explicitly configured as \texttt{0000}.}

\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Opcode} & \textbf{Name} & \textbf{Operational Action / Mechanics} \\ 
\midrule
U0000 & Swap Registers & (M Register) $\longleftrightarrow$ (Accumulator) \\
B0000 & Set Plus Sign & Forces (Accumulator) positive \hfill [Absolute Value: $|Accumulator|$] \\
T0000 & Set Minus Sign & Forces (Accumulator) negative \hfill [Inverse Absolute Value: $-|Accumulator|$] \\
Y0000 & Change Sign & (Accumulator) $\times -1 \rightarrow$ Accumulator \hfill [Toggle sign bit] \\
Z0000 & Operational Stop & Halts execution if console switch \textsf{PS-16} is \textsf{ON}. Resumes on manual \textsf{START}. \\
E0000 & Exit Interpreter & Safe exit from Virtual Machine; returns to native hardware control at next location. \\
\bottomrule
\end{tabularx}
\end{table}

\paragraph{Input/Output Instructions}
\textit{Note: The address field for all input/output commands must be set to \texttt{0000}.}

\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Opcode} & \textbf{Name} & \textbf{Operational Action / Mechanics} \\ 
\midrule
I0000 & Data Input & Diverts to tape-reader module. Parses, normalizes, and stores decimal variables. \\
P0000 & Data Output & Prints/Punches the current Accumulator value. Accumulator data remains unaltered. \\
\bottomrule
\end{tabularx}
\end{table}

During tape data input (\texttt{I0000}), sequential tracking remains under tape control until a designated physical stop-code is pulled from the reader, passing execution control back to the next sequential interpreter instruction.

\paragraph{Function Instructions}
\textit{Note: The address field for all transcendental operations must be set to \texttt{0000}. The input argument must already sit in the Accumulator.}

\begin{table}[h]
\centering
\small
\ttfamily
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Opcode} & \textbf{Name} & \textbf{Operational Action / Mechanics} \\ 
\midrule
R0000 & Square Root & $\sqrt{\text{(Accumulator)}}$ $\rightarrow$ Accumulator. Negative inputs trigger an immediate error halt. \\
S0000 & Sine & $\sin\text{(Accumulator)}$ $\rightarrow$ Accumulator. Input argument must be formatted in radians. \\
Q0000 & Cosine & $\cos\text{(Accumulator)}$ $\rightarrow$ Accumulator. Input argument must be formatted in radians. \\
A0000 & Arctangent & $\arctan\text{(Accumulator)}$ $\rightarrow$ Accumulator. Resulting output is scaled in radians. \\
N0000 & Natural Logarithm & $\ln\text{(Accumulator)}$ $\rightarrow$ Accumulator. \\
H0000 & Exponential & $e^X \rightarrow$ Accumulator, where $X$ represents the initial value held in the Accumulator. \\
\bottomrule
\end{tabularx}
\end{table}

\bigskip
\textbf{System Composition Architecture Note:} The core arithmetic instructions, branch/jump vectors, address register manipulation routines, auxiliary commands, and the primary Square Root (\texttt{R0000}) engine form the permanent hardcoded baseline footprint of the interpreter core loop. 

Conversely, the individual Data Input, Data Output, and specialized transcendental math packages were developed as independent software library modules. To maximize available user space on the drum, these external extensions only need to be loaded into memory if the user's application program explicitly calls them.

\subsubsection*{INPUT}

The pseudo-input instruction (\texttt{I0000}) requires a data paper tape formatted precisely as follows:
\begin{enumerate}
    \item A starting address block.
    \item The raw data formatted as signed decimal integers coupled with their respective exponents.
    \item A designated group-termination code at the end of each distinct cluster of numbers.
    \item A final master execution stop-code at the terminal end of the data tape.
\end{enumerate}

\paragraph{The Starting Address}
Every independent data group must begin with its target starting memory location, designated in decimal Track/Sector notation. This must be an absolute address and must be immediately followed by a physical stop-code (\texttt{'}). 

\textit{Example:} The entry \texttt{4200'} instructs the input subroutine to begin storing the incoming converted floating-point variables sequentially starting at location \texttt{4200}.

\paragraph{The Data Structure}
Following the starting address line, one or more signed decimal integers and their exponents are fed into the reader. The subroutine converts this decimal matrix into internal binary floating-point representations and stores them in consecutive, two-location blocks on the drum.

Each individual floating-point value must be explicitly split into \emph{two distinct words} on the paper tape, each line capped with a hardware stop-code (\texttt{'}). 
\begin{itemize}
    \item \textbf{Negative values:} The minus sign (\texttt{-}) must immediately precede the very first digit of the first word.
    \item \textbf{Positive values:} A space may be inserted before the first digit, but a physical plus sign (\texttt{+}) is strictly prohibited.
\end{itemize}

The structural payload layout across the tape lines is split as follows:
\begin{itemize}
    \item \textbf{Word 1:} Contains the algebraic sign bit and the first seven digits of the decimal integer.
    \item \textbf{Word 2:} Contains the final two digits of the decimal integer, followed immediately by the sign and the two digits of the base-10 exponent.
\end{itemize}

Formally, the physical tape notation maps to this exact structural syntax:
\begin{center}
\ttfamily
\begin{tabular}{ll}
\textnormal{\textbf{Line 1 (Word 1):}} & $\pm d_1d_2d_3d_4d_5d_6d_7'$ \\
\textnormal{\textbf{Line 2 (Word 2):}} & $d_8d_9\pm e_1e_2'$ \\
\end{tabular}
\end{center}
Where $\pm d_1d_2d_3d_4d_5d_6d_7d_8d_9$ constitutes the total 9-digit signed decimal integer, and $\pm e_1e_2$ represents the 2-digit decimal scaling exponent.

\medskip
The mathematical value of the processed data variable is determined by the underlying relationship:
\[ \text{Represented Value} = \text{Integer} \times 10^{E_{10}} \]
Where the exponent boundaries are hardcoded at: $-99 \le E_{10} \le +99$.

\paragraph{Input Encoding Examples}
\begin{itemize}
    \item \textbf{Example 1:} Consider the fraction $0.0900000521374209$. This corresponds mathematically to the scientific notation expression: $521374209 \times 10^{-16}$. To feed this value into the interpreter, it must be punched onto the paper tape as two distinct lines:
    \begin{verbatim}
    5213742'
    09-16'
    \end{verbatim}
    
    \item \textbf{Example 2:} Consider the large whole number $5522829640000000$. This corresponds to the mathematical notation: $552282964 \times 10^{+07}$. It must be presented to the reader as:
    \begin{verbatim}
    5522829'
    64+07'
    \end{verbatim}
\end{itemize}

\paragraph{Termination Stopcodes}
The system defines two distinct classes of termination stop-codes. Their behavior varies fundamentally based on their context and placement on the data coding sheet:
\begin{enumerate}
    \item \textbf{Group Termination Code:} Signifies the end of the current numerical data block. It commands the system to immediately begin reading the next subsequent address and data group on the tape.
    \item \textbf{Master Input Stop-Code:} Signifies the total completion of data entry. It triggers an automatic terminal carriage return, shuts down the tape reader pipeline, and gracefully jumps execution control back to the next sequential line of the user's main program.
\end{enumerate}

\paragraph{Input example}
Table \ref{data-input-table} demonstrates how data should be punched to be read by the \texttt{I0000} instruction. Data is entered in groups, starting with the address where the data should be stored, followed by the (signed) mantissa, a space, and the (signed) exponent, followed by a stopcode to terminate the number. The input routine will continue reading numbers until a lone stopcode is entered, ending the group. If two lone stopcodes are entered, input ends and execution resumes immediately following the \texttt{I0000} instruction.

\begin{center}
\small
\ttfamily
\begin{tabular}{crcccccrccrcc}
\toprule
&\textbf{INPUT CONTROL WORD} & \textbf{S} & \multicolumn{4}{c}{\textbf{SIGNED DECIMAL NUMBERS}} &  & \textbf{S} & &\\
&(Starting Address)          & \textbf{T} &   &Part 1&'& Part 2                     &  & \textbf{T} & &\\
&                            & \textbf{O} & &$d_1d_2\dots d_7$ & & $d_8d_9 \ e_1e_2$          &  & \textbf{O} & &\\
&                            & \textbf{P} &                   &                         &     &   &  & \textbf{P} & &\\
\midrule
1&5500                        & '$^{\dagger}$          & - & 3300000           &'& 00~-&04                        & '          & \hfill [Group 1] &\\
2&                            &            &   & 3141592$^{\ddagger}$           &'& 65~-&08                        & '          & &\\
3&                            &            & - & 0000592           &'& 34~-&11                        & '          & &\\
4&							&            &   &                   &'$^{\P}$&                                  &           & &\\
\midrule
5&2134                        & '          &   & \phantom{000000}0 &'&                              &      & '          & \hfill [Group 2] &\\
6&                            &            &   & \phantom{00000}30 &'&50~-&\phantom{00}               & '          & &\\
7&                            &            &   & \phantom{0000}750 &'&00~+&\phantom{00}               & '          & &\\
8&                            &            & - & 0100000           &'&00~+&\phantom{00}               & '          & &\\
9&							&            &   &                   &'$^{\S}$&                                &  &'$^{\parallel}$           & &\\
\bottomrule
\end{tabular}
\vspace{1em}
\captionof{table}{Sample Data Input Format}
\label{data-input-table}
\end{center}

\vspace{1em}
\nopagebreak
\medskip
$^{\dagger}$ The stopcode (\texttt{'}) immediately following the initial address ends the address and starts number entry for the input group.

\medskip
$^{\ddagger}$ A single stopcode with no numeric input terminates the input group.

\medskip
$^{\P}$ Positive quantities are encoded with \emph{no} prefix; negative values should have an explicit minus sign (\texttt{-}). Do not punch a plus sign (\texttt{+}) for positive values.

\medskip
$^{\S}$ This single stopcode ends the second data group.

\medskip
$^{\parallel}$ This stopcode following the group terminator stopcode tells the interpreter to exit input mode and resume execution.

\medskip

Reading the data in Table \ref{data-input-table} results in the following data in memory. Group 1 is read and the floating-point numbers are built\footnote{Translator's note: I've preserved the original manual's representation here; suffice it to say that the values indicated are handwaving the actual values in memory.} starting at location \texttt{5500}:

\texttt{5500} \texttt{-33000.0} \\
\texttt{5501} \textit{(Exponent for $-33000.0$)} \\
\texttt{5502} \texttt{3.14159265} \\
\texttt{5503} \textit{(Exponent for $3.14159265$)} \\
\texttt{5504} \texttt{-0.00000059234} \\
\texttt{5505} \textit{(Exponent for $-0.00000059234$)}

All exponents are strictly maintained at $q = 29$. The physical stopcode trailing the final number of the group (located in the column immediately following Group 1) triggers the system to automatically advance and read the next starting target address of the second data group (\texttt{2134}\footnote{Note that these addresses don't need to be ordered. The input routine will happily build two-word floating-point numbers wherever you indicate.}). The second group is processed and stored as follows:

\texttt{2134} $\rightarrow$ \texttt{Zero (0.0)} \\
\texttt{2135} $\rightarrow$ \texttt{Zero (0.0)} \\
\texttt{2136} $\rightarrow$ \texttt{30.50} \\
\texttt{2137} $\rightarrow$ \textit{(Exponent for $30.50$)} \\
\texttt{2138} $\rightarrow$ \texttt{750000.0} \\
\texttt{2139} $\rightarrow$ \textit{(Exponent for $750000.0$)} \\
\texttt{2140} $\rightarrow$ \texttt{-10000000000000.0} \\
\texttt{2141} $\rightarrow$ \textit{(Exponent for $-10000000000000.0$)}

\vspace{1em}
The processing loop for the second data group is terminated by a single stop-code just as Group 1 was. Note the \emph{second} stopcode, which serves to signal the interpreter that input is complete and execution may continue.

\medskip
\textbf{Special case:} Please note that any number meant to be input as a literal value of zero \emph{must} be explicitly encoded and punched onto the tape as \texttt{0'} rather than as just a stopcode (\texttt{'})\footnote{This is different from other input routines, particularly the Program Input 1 (Section~\ref{sec:PIRloader}) loader.}. Explicitly entering the zero values is necessary to allow the input routine to differentiate a true numerical zero from a structural group-termination/input-termination command.

\subsubsection*{OUTPUT}
The output format works to make the numbers a consistent size on the page, and to have a consistent format. Each number has:
\begin{itemize}
	\item a leading character for the sign. This will be a space for positive numbers, and $-$ for negative ones,
	\item the mantissa of the floating-point number, in leading-decimal format,
	\item a separator space,
	\item the sign of the exponent (this is either $+$ or $-$, and \emph{always} appears),
	\item the two-digit exponent itself, and
	\item a tab character.
\end{itemize}

The \texttt{space}/$-$ convention means that positive and negative numbers will align properly to the left down a column.

The tab character enables the user to easily create well-formatted output on the Flexowriter by setting the hardware tab stops.

Control is immediately returned to the main program loop after printing is complete. A single floating-point number is processed and output per subroutine invocation. The actual printed format of a number looks like this:

\begin{center}
\small
\begin{tabular}{rl}
% Row 1: The printed text format string (with explicit leading space)
Positive&\texttt{\phantom{-}.xxxxxxx\ $\pm$ee[Tab]} \\
Negative&\texttt{-.xxxxxxx\ $\pm$ee[Tab]} \\
\end{tabular}
\end{center}

\medskip
This physical teleprinter serialization format represents the standard mathematical expression:
\begin{equation*}
\pm\text{.\texttt{xxxxxxx}} \cdot 10^{\pm\text{\texttt{ee}}}
\end{equation*}

\paragraph{Output Examples}
The following examples illustrate how specific numerical values are printed:

\begin{center}
\small
\ttfamily
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Printed Output} & \textbf{Mathematical Value} & \textbf{Notes} \\
\midrule
-.5060000+04            & -5060.0                     & Negative mantissa, positive exponent \\
\phantom{-}.9375000-02  & 0.009375                    & Leading space for positive mantissa \\
-.1000000-06            & -0.0000001                  & Extreme fractional negative \\
\phantom{-}.1000000+30  & $1.0 \times 10^{30}$        & Large positive value \\
\bottomrule
\end{tabularx}
\end{center}

\medskip
Note how the leading column remains perfectly uniform; the decimal points align vertically on the page regardless of whether the value carries a negative sign or an implicit positive blank space.

\paragraph{Output Limits and Constraints}
The formatting routine enforces the following mathematical boundaries during execution:
\begin{itemize}
	\item \textbf{Decimal Exponent Range:} $-99 \le ee \le +99$.
	\item \textbf{Decimal Mantissa Bounds:} $0.1000000 \le \lvert\text{mantissa}\rvert \le 0.9999999$, or exactly zero.
	\item \textbf{Internal Binary Exponent Range:} $-333 \le \text{exponent} \le +333$.
\end{itemize}

An underflow condition occurs when where the internal binary exponent drops below $-333$; this causes the routine to automatically print a clean zero. Conversely, an overflow condition where the binary exponent exceeds $+333$ triggers a terminal range error stop, halting the interpreter loop.

\subsubsection*{FUNCTION SUBROUTINES}

\paragraph{SINE-COSINE}
This subroutine calculates the sine or cosine of a given value expressed in radians, using a 9th degree polynomial to approximate the value. To use, load the angle into the floating-point accumulator then execute the appropriate pseudo-instruction (\texttt{S0000} for sine or \texttt{C0000} for cosine). The result is placed in the floating-point accumulator. The following table shows the method for calculating the function values depending on the exponent of the argument.

\begin{center}
\small
\begin{tabularx}{\textwidth}{lllX}
\toprule
\textbf{Exponent (binary)} & \textbf{Sine X} & \textbf{Cosine X} & \textbf{Notes}\\
\midrule
+26 to +127 & Range error & Range error & \\
-8 to +25   & Polynomial evaluation & Polynomial evaluation & \\
-20 to -9   & Sine X = X & Polynomial evaluation & $\sin X \approx X$ for small angles \\
-127 to -21 & Sine X = 0 & Cosine X = 1.0 & Exceeds precision \\
\bottomrule
\end{tabularx}
\end{center}

\paragraph{ARCTANGENT}
This subroutine calculates the angle that corresponds to a given tangent argument. Load the tangent value into the floating-point accumulator and execute the \textbf{A0000} pseudo-instruction. The result is returned in radians in the floating-point accumulator.

\paragraph{LOGARITHM AND EXPONENTIAL FUNCTION}
Load the independent variable \texttt{X} into the floating-point accumulator and execute the appropriate pseudo-instruction (\texttt{N0000} for $log_e$ and \texttt{H0000} for $e^x$); the resulting value is returned in the floating-point accumulator.

\paragraph{SQUARE ROOT}
This subroutine, which is included in the interpreter system, calculates the square root of the number in the floating-point accumulator and returns it there.

\bigskip
\hrule
\bigskip

\subsubsection*{OPERATION}
Input is floating-point numbers loaded from tape or in memory, or from numbers in the pseudo-registers from previous operations.

\paragraph{Running the interpreter}
Entry to the interpreter is via a subroutine call using an \texttt{R}/\texttt{U} sequence. Similar to the Floating-point Interpreter I,
the \texttt{R} instruction sets the address portion of a \texttt{B} instruction at $L_0$, which in this case acts as an executable pointer back
to the pseudo-instructions inline after the \texttt{U}. The interpreter extracts the address of the code to be interpreted from this \texttt{B}
instruction. It does \emph{not} set a return address, but instead uses the \texttt{E0000} instruction to detect the end of the interpreted
pseudo-instructions, and branches back to the (fixed-point) instruction following the end of the floating-point code.

\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha$             & \texttt{R}                    & $L_0$            & Store starting address of floating-point instructions. \\
$\alpha+1$           & \texttt{U}                    & $L_0$            & Entry into the interpreter system. \\
$\alpha+2$           & \multicolumn{2}{c}{$\langle$\dots$\rangle$}               & Floating-point instructions. \\
$\alpha+3$           & \multicolumn{2}{c}{$\langle$\dots$\rangle$}               & \\
$\alpha+n$           & \texttt{E}                    & \texttt{0000}             & Exit from the interpreter system.\\
$\alpha+n+1$         & \multicolumn{2}{c}{$\langle$\dots$\rangle$}               & Resume fixed-point instructions. \\
\bottomrule
\end{tabularx}
\end{table} 

\paragraph{Memory Requirements}
If the starting address of \texttt{H1-11.1} is defined as $L_0$, then the routines on this tape must be loaded as follows (using the corresponding input and modifier key words).

\begin{center}
\small
\begin{tabular}{llll}
\toprule
\textbf{Program} & \textbf{Input key word} & \textbf{Modifier key word} & \textbf{Memory required} \\
\midrule Interpreter system & $L_0$      &   & 11 tracks \\
Data input           & $L_0+1100$ & $L_0$ & 6 tracks \\
Data output          & $L_0+1700$ & $L_0$ & 4 tracks \\
Sine - Cosine        & $L_0+2100$ & $L_0$ & 2 tracks \\
Arctangent           & $L_0+2300$ & $L_0$ & 83 sectors \\
Logarithm            & $L_0+2419$ & $L_0$ & 45 sectors \\
Exponential function & $L_0+2500$ & $L_0$ & 2 tracks \\
\bottomrule
\end{tabular}
\end{center}

\textbf{Scratch Storage} --- The program uses locations 6300 to 6363 as working storage.

\paragraph{Program Stops}
\begin{center}
\small
\begin{tabularx}{\textwidth}{l p{4.5cm} X}
\toprule
\textbf{Memory Location} & \textbf{Meaning} & \textbf{Remedy / Action} \\
\midrule
$L_0+0947$ & Negative argument for square root & Press \textsf{START}. The pseudo-instruction exits with the original argument in the accumulator. \\
\midrule
$L_0+0312$ & Exponent 333 for Hold or Clear & Press \textsf{START}. Hold or Clear pseudo-instructions are bypassed. \\
\midrule
$L_0+0710$ & Division by zero or non-floated number & Execution cannot continue. \\
\midrule
$L_0+1808$ & Exponent > 99 during data input & Press \textsf{START}. The Flexowriter tabs to the next stop without printing, and the code exits to the core interpreter. \\
\midrule
$L_0+2100$ & Argument for $\sin$/$\cos \ge 2^{26}$ & Press \textsf{START}. The \texttt{S0000} or \texttt{C0000} operation is skipped. \\
\midrule
$L_0+2426$ & Argument for $\log_e X \le 0$ & Press \textsf{START}. The pseudo-instruction returns zero and control returns to the main program. \\
\midrule
$L_0+2629$ & Argument for $e^X \ge 128$ & Press \textsf{START}. The pseudo-instruction is skipped. \\
\bottomrule
\end{tabularx}
\end{center}

\paragraph{Program Control Switches}
The \textsf{PS-16} switch on the console can be used as a ``breakpoint'' facility to halt execution at selected points.
\begin{center}
\small
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Console Switch} & \textbf{Setting} & \textbf{Action} \\
\midrule
\textsf{PS-16} & \textbf{ON}  & Execution stops at \texttt{Z0000} instructions. \\
               & \textbf{OFF} & Execution continues past \texttt{Z0000} instructions. \\
\bottomrule
\end{tabularx}
\end{center}

\paragraph{Precision and Accuracy}
\begin{itemize}
	\item \textbf{Sine-Cosine:} Maximum error is $5 \times 10^{-8}$.
	\item \textbf{Data Output:} The maximum error is limited to:
	\begin{itemize}
		\item $1$ unit in the last decimal place of the mantissa if $\text{EXP}_{10} \le 30$.
		\item $5$ units in the last decimal place of the mantissa if $\text{EXP}_{10} \le 99$.
	\end{itemize}
\end{itemize}

\textbf{Translator's note:} To print a floating-point number, the data output routine has to convert a binary exponent ($2^x$) into a decimal exponent ($10^y$). Mathematically, this requires multiplying or dividing the mantissa by powers of 10 (or powers of $\log_{10}(2)$) until the binary scaling is cleared out.
\begin{enumerate}
	\item For $\le 30$: For these exponents, the conversion can be done using exact or very tightly bounded multiplication factors that fit cleanly within a single or double-word of 31-bit precision. The rounding error is kept to a minimum of 1 unit in the last place.
	\item For $> 30$: For very large numbers (up to $10^{99}$), the output routine has to repeatedly multiply by large powers of 10. Because of the LGP-21's 31-bit word size, the intermediate multiplication constants themselves have to be rounded to fit. As the algorithm repeatedly loops and multiplies to scale down large exponents, the rounding errors in these constants also multiply.
\end{enumerate}

By the time the conversion routine finishes working through a number like $10^{95}$, the accumulated rounding error corrupts the bottom bits of the mantissa, producing an output that could be off by up to 5 units in the 7th decimal digit.

\subsubsection*{REMARKS}

\begin{enumerate}
\item A program can exit H1-11.1 interpreter mode and return to it without the contents of the pseudo-registers being lost.
\item The starting address of the system should be at sector 00 of a track. Otherwise, many of the addresses that refer to track 63 are not optimal.
\item It's good practice to load the entirety of \texttt{H1-11.1}, and to then punch out the individual sub-libraries by means of \texttt{J4-10.2} or \texttt{J4-10.3}. This enables you to load only the parts that you require using \texttt{J1-10.0} or by means of \texttt{J5-10.0}. This allows you to verify checksums on load.
\item Instructions with zero addresses do not actually refer to location 0000, but are used to indicate special pseudo-instructions. The interpreter has 14 pseudo-instructions with zero addresses, plus the two special shift instructions \texttt{D000y} and \texttt{M000y}. These instructions set their address part to values from 0001 to 0009, meaning that the division and normal multiplication instructions must not use these addresses in interpreter mode.
\end{enumerate}

\begin{tabularx}{\textwidth}{lX}
	\textbf{INSTRUCTION} & \textbf{RESULT, IF ADDRESS XXXX $\ne$ 0} \\
	\midrule
	\texttt{Z} & (Address reg.) $-$ (\texttt{XXXX}) $= 0$? yes $\rightarrow$ skip next instruction; no $\rightarrow$ do not skip \\
	\texttt{B} & (\texttt{XXXX}) $\rightarrow$ (Accumulator) \\
	\texttt{Y} & (Address reg.) $\rightarrow$ address part of (\texttt{XXXX}) \\
	\texttt{R} & Address of R plus 2 $\rightarrow$ address part of (\texttt{XXXX}) \\
	\texttt{I} & (Address reg.) $+$ (\texttt{XXXX}) $\rightarrow$ (Accumulator) \\
	\texttt{D} & (Accumulator) $/$ (\texttt{XXXX}) $\rightarrow$ (Accumulator) \\
	\texttt{N} & (M Reg.) $\times$ (\texttt{XXXX}) $+$ (Accumulator) $\rightarrow$ (Accumulator) \\
	\texttt{M} & (M Reg.) $\times$ (\texttt{XXXX}) $\rightarrow$ (Accumulator) \\
	\texttt{P} & (\texttt{XXXX}) $\rightarrow$ (M Reg.) \\
	\texttt{E} & Address part of (\texttt{XXXX}) $\rightarrow$ address part of the (Accumulator) \\
	\texttt{U} & Take next instruction from \texttt{XXXX} \\
	\texttt{T} & Jump if (Accumulator) is negative \\
	\texttt{-T} & Jump if (Accumulator) is negative or if PST is turned on \\
	\texttt{H} & (Accumulator) $\rightarrow$ (\texttt{XXXX}) \\
	\texttt{C} & (Accumulator) $\rightarrow$ (\texttt{XXXX}); 0 $\rightarrow$ (Accumulator) \\
	\texttt{A} & (Accumulator) $+$ (\texttt{XXXX}) $\rightarrow$ (Accumulator) \\
	\texttt{S} & (Accumulator) $-$ (\texttt{XXXX}) $\rightarrow$ (Accumulator) \\
\end{tabularx}

\vspace{0.5cm}

\begin{tabularx}{\textwidth}{lX}
	\textbf{INSTRUCTION} & \textbf{RESULT, IF ADDRESS = 0 *} \\
	\midrule
	\texttt{Z} & Stop (PS-16); continue calculating after \textsf{START} is pressed \\
	\texttt{B} & Make (Accumulator) positive \\
	\texttt{Y} & Form complement of the (Accumulator) \\
	\texttt{R} & $\sqrt{\texttt{(Accumulator)}} \rightarrow$ (Accumulator) \\
	\texttt{I} & Input of floating-point data \\
	\texttt{D} & (Accumulator) $/ 2^y \rightarrow$ (Accumulator) [address is not 0000, but 0001 to 0009] \\
	\texttt{N} & $\text{Log}_e$ (Accumulator) $\rightarrow$ (Accumulator) \\
	\texttt{M} & (Accumulator) $\times 2^y \rightarrow$ (Accumulator) [address is not 0000, but 0001 to 0009] \\
	\texttt{P} & Print (Accumulator) \\
	\texttt{E} & Exit interpreter mode \\
	\texttt{U} & Swap (Accumulator) and (M Reg.) \\
	\texttt{T} & Make (Accumulator) negative \\
	\texttt{-T} & Jump if (Accumulator) is negative or if \textsf{PST} switch is on \\
	\texttt{H} & $e^{\text{(Accumulator)}} \rightarrow$ (Accumulator) \\
	\texttt{C} & Cosine (Accumulator) $\rightarrow$ (Accumulator) \\
	\texttt{A} & Arctangent (Accumulator) $\rightarrow$ (Accumulator) \\
	\texttt{S} & Sine (Accumulator) $\rightarrow$ (Accumulator) \\
\end{tabularx}

\paragraph{Coding sheet for Horner's Method example}
[\textbf{Translator's note:} This coding example and its data are simply oddly bunged on to the end of the chapter; best guess is that someone wanted a coding example of how to use the interpreter, and someone threw together a quick demo.]

\begin{tabular}{|p{0.15\textwidth}|c|p{0.06\textwidth}|p{0.15\textwidth}|c|p{0.15\textwidth}|p{0.25\textwidth}|}
	\hline
	\textbf{Input} & \textbf{S} & \textbf{Loc.} & \textbf{Opcode} & \textbf{S} & \textbf{Address} & \textbf{Comments} \\
	\textbf{keyword} & \textbf{T} & & \textbf{\& Addr.} & \textbf{T} & \textbf{Contents} & \\
	& \textbf{O} & & & \textbf{O} & & \\
	& \textbf{P} & & & \textbf{P} & & \\
	\hline
    ;0004200 & ' & & & & & Word counter to Track 04, Sector 00. \\
	/0004200 & ' & & & & & Origin to Track 04, Sector 00. \\
		      & & 00 & xR 1400 & ' & & Set interpreted section pointer in interpreter\\
		      & & 01 & xU 1400 & ' & & Begin interpreting floating-point code \\
		      & & 02 & xI 0000 & ' & & Read coefficients from Flexowriter (see below)\\
		      & & 03 &  E 0018 & ' & 4306 & Set starting \\
		      & & 04 &  Y 0006 & ' & 4306 & address \\
		      & & 05 &  B 0013 & ' & Zero & \\
		      & & 06 &  A [    ] & ' & $a_n$ & This address will be overwritten \\
		      & & 07 & xL 0002 & ' & $a(4306+2n)$ & Increment address \\
		      & & 08 &  Y 0006 & ' & $a(4306+2n)$ & register by 2 \\
		      & & 09 & xZ 4316 & ' & & Check for completion \\
		      & & 10 &  U 0015 & ' & & Not finished \\
		      & & 11 & xP 0000 & ' & & Print result before exit \\
			  & & 12 & xE 0000 & ' & & Exit interpreter \\
			  & & 13 & xZ 0000 & ' & & Stop machine (fixed-point opcode) \\
			  & & 14 &  U 0000 & ' & & Swap accumulator into instruction counter \\
			  & & 15 & xU 0000 & ' & & Swap Accu. / M. Reg. \\
			  & & 16 & xM 4304 & ' & & Multipl. by $x$ \\
			  & & 17 &  U 0006 & ' & & Return for next term \\
			  & & 18 & xZ 4306 & ' & & Initial coefficient \\
			  000XXXX & & 19 & & ' & & \\
		\hline
\end{tabular}

\paragraph{Data input sheet for Horner's Method}
This is sample data in the format that Horner's Method demo program expects.
\begin{center}
\small
\ttfamily
\begin{tabular}{|c|r|c|c|c|c|c|c|c|c|c|c|}
	\hline
	& \textbf{INPUT CONTROL WORD} & \textbf{S} & \multicolumn{4}{c|}{\textbf{SIGNED DECIMAL NUMBERS}} &   & \textbf{S}\\
	& (Starting Address)          & \textbf{T} &         & Part 1 & ' & Part 2                        &   & \textbf{T}\\
	&                              & \textbf{O} &   & $d_1d_2\dots d_7$ & & $d_8d_9$         &$e_1e_2$   & \textbf{O}\\
	&                              & \textbf{P} &   &        &   &                                    &   & \textbf{P}\\
	\hline
	1 & 4304                       &'&   & 1090000 & ' & 00~+ & 09           & ' \\
	2 &                            & &   & 0120000 & ' & 00~- & 09           & ' \\
	3 &                            & &   & 0340000 & ' & 00~- & 09           & ' \\
	3 &                            & &   & 0560000 & ' & 00~- & 09           & ' \\
	3 &                            & &   & 0780000 & ' & 00~- & 09           & ' \\
	3 &                            & &   & 0900000 & ' & 00~- & 09           & '\\
	9 &                            &'&   &         &   &                     &  & '\\
	\hline
\end{tabular}
\end{center}

\subsubsection*{Translator's Notes}

\paragraph{Why do we have \emph{two} floating point packages?}
Modern programmers would look at this documentation and immediately start trying to figure out how to get rid of one of these two packages. Why would anyone want, or need, two separate floating point packages?

The following is speculation, based off the architectures of the two interpreters and years of experience with human nature, so take it with a grain of salt. If we ever have a real LGP-21 programmer around to ask, who actually \emph{used} both, we can nail it down for sure. But having said that, it appears that there two different floating-point interpreters because there were two different audiences who needed floating point.

The first set of users would be people who are familiar with the LGP-21 and the fixed-point frctional architecture; who are comfortable thinking about numbers in terms of $q$ and who have relatively lightweight requirements for floating-point operations. They will be writing code mostly in LGP-21 native mode, and occasionally dropping into floating point for a few calculations. Basic I/O of floating point works fine for them. Interpreter 1 is their choice.

The second set of users are primarily engineers, mathematicians, scientists...anyone whose native way of thinking about math is ``things you do with real numbers''. They want an instruction set that natively Does The Thing, using floating point numbers. Power-of-10 scaling makes perfect sense to them, but bit-twiddling and screwing around with representation is a distraction from the work they need to get done. Their attitude is effectively: \emph{``I just want to work with actual numbers. These fixed-point fractions are too weird.''}

There are tradeoffs; we can see some of them from the descriptions of the two interpreters:
\begin{itemize} 
	\item Interpreter 2 has a pad word following each mantissa-exponent pair, interpreter 1 doesn't. Is this because the all-float interpreter 2 runs \emph{slower} than the limited interpreter 1? Or is it a brilliant speed hack that makes it run \emph{faster}? 
	\item Code written for interpreter 1 is \emph{probably} faster, up to a point, but after the ratio of fixed-point to floating-point operations tilts far enough toward floating-point, the overhead to move in and out of floating-point mode and the overhead to move \emph{data} in and out will eventually make the interpreter 1 program larger than the same program written for interpreter 2, which doesn't have the mode-switch overhead, and eventually \emph{may} make it slower.
	\item An Interpreter 2 program may be slower than an Interpreter 1 program, but it is likely to be \emph{smaller} because the mode-switch overhead is no longer needed.
\end{itemize}

We won't know for sure until we find a programmer who used both, or two programmers who have had a 50-plus year argument about which is better.

\paragraph{Why interpreters?}
A modern programmer's second thought is ``why are the LGP-21's floating point implementations \emph{interpreters?}'' Couldn't we just write some nice libraries and call it a day?

On a more modern machine with a stack or other good support for subroutine calls, sure. (We'll pretend it doesn't have native floating point.) Let's say we're doing \texttt{fmul(fadd(a,b),fadd(c+d))}. We'd put a on the stack, call the conversion routine, push b on the stack, convert it, call the float-add function, put c on, convert, put d on, convert, call float-add, call float-multiply, done!

On the LGP-21, there are \emph{so many} more operations. We have no stack, so everything has to go in temporary storage. We can't hold a two-word (mantissa and exponent) number in the regular accumulator, so we'll have to do something like this\footnote{This is only the instructions needed to set up to \emph{do} the operations; we're skipping the actual functions and the wiring necessary to do things like set up return addresses, find arguments, etc. Those are assumed to be library overhead and not something the caller codes...but that overhead exists too.}:

\begin{tabularx}{\textwidth}{llX}
	\textbf{Operation} & \textbf{Estimated} & \textbf{Description} \\
	                   & \textbf{instructions} & \\
	\texttt{fread(a)} & 3 & read value for \texttt{a} \\
	\texttt{fread(b)} & 3 & read value for \texttt{b} \\
	\texttt{fread(c)} & 3 & read value for \texttt{c} \\
	\texttt{fread(d)} & 3 & read value for \texttt{d} \\
    \texttt{fadd(a, b, t1)} & 3 & \texttt{a + b} to \texttt{t3}\\
    \texttt{fadd(c, d, t2)} & 3 & \texttt{c + d} to \texttt{t4}\\
	\texttt{fmul(t1, t2, t1)} & 3 & Final result in \texttt{t1}\\
	\texttt{print(t1)} & 3 & Print it	\\
\end{tabularx}

Assuming that we can use a \texttt{B}/\texttt{R}/\texttt{U} calling sequence for each of these operations, that's 24 instructions to evaluate the expression; 4 words to store the values of \texttt{a}, \texttt{b}, \texttt{c}, and \texttt{d} that we read; and 6 for temp storage \texttt{t1}, \texttt{t2}, and \texttt{t3}; a total of 34 words. If we use an interpreter that ``natively'' understands floating point, we can do this:

\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha$     & \texttt{R} & $L_0$           & Start of interpreter. \\
$\alpha+1$   & \texttt{U} & $L_0$           & Entry into the interpreter system. \\
$\alpha+2$   & \texttt{I} & \texttt{0000}  & Read \texttt{a}, \texttt{b}, \texttt{c}, \texttt{d} as float. \\
$\alpha+3$   & \texttt{B} & \texttt{a}     & Load \texttt{a}. \\
$\alpha+4$   & \texttt{A} & \texttt{b}     & Add \texttt{b}. \\
$\alpha+5$   & \texttt{H} & \texttt{t1}    & Store result in \texttt{t1}. \\
$\alpha+6$   & \texttt{B} & \texttt{c}     & Load \texttt{c}. \\
$\alpha+7$   & \texttt{A} & \texttt{d}     & Add \texttt{d}. \\
$\alpha+8$   & \texttt{H} & \texttt{t2}    & Store result in \texttt{t2}. \\
$\alpha+9$   & \texttt{B} & \texttt{t1}    & Load \texttt{t1}. \\
$\alpha+10$  & \texttt{A} & \texttt{t2}    & Multiply \texttt{t2}. \\
$\alpha+11$  & \texttt{P} & \texttt{0000}  & Print result. \\
$\alpha+12$  & \texttt{E} & \texttt{0000}  & Exit from the interpreter. \\
$\alpha+n+1$ & \multicolumn{2}{c}{\dots}     & Resume fixed-point. \\
\bottomrule
\end{tabularx}
\end{table}

To execute the exact same set of operations, the interpreter version only takes\emph{12} (pseudo) instructions plus 10 words to store 5 floating-point numbers, 22 total, and is much simpler to understand. Fitting your program into much less storage is a serious consideration on a machine with only 4K words, especially since you'll be giving up memory to your libraries or your interpreter. (Interestingly, it should be noted that for Interpreter 2, the floating-point library functions are \emph{smaller} than the corresponding fixed-point fractional ones.)
\paragraph{Coding example for Interpreter 2}
This is a far better example of using all of the facilities of Interpreter 2 than the very simple comparison program above.

The Interpreter 2 Horner's Method example shows how a program would have been coded on an LGP-21 coding sheet, and then fed to one of the program loaders in \ref{sec:program-loaders} to make it resident in memory.

The program as designed is set up to allow the user to run the program, enter a set of exactly 5 coefficients (we're not up to handling dynamic loops here), and have them multiplied out:
$$f(x) = a_4x^4 + a_3x^3 + a_2x^2 + a_1x + a_0$$
\paragraph{Reading an LGP-21 coding sheet}
We've elected to keep the coding sheet for this particular example instead of writing out assembler code because it's useful to see how an LGP-21 program was coded by hand.
Note that the locations start at 00 and go up; these line numbers act as ``virtual'' memory addresses, and can be referred to by other instructions by number, very much like a BASIC program. Instead of a \texttt{GOTO 200}, you have a \texttt{U 0015} to branch to the instruction on line 15.

The instructions with a leading \texttt{x} are \emph{non-relocated}; their addresses are not modified when the program is loaded. For example, lines 0 and 1 set up Interpreter 2 by saving the address of the instruction 2 on from line 0 into the interpreter itself, at the fixed address 1400. Additionally, the interpreter's special \texttt{I} and \texttt{P} instructions, to read and write data, have to have an address field of exactly 0000 to work properly, so those instructions are also marked as non-relocatable.

These two items together are key to understanding how the program works. A more modern\footnote{Or at least as modern as you're going to get for a computer built in 1963} assembler would have had labels for the line-number branches that the coding sheet uses, but would still emit assembled code that looks more like the coding sheet than anything else.
\paragraph{How the Horner's Method coding sheet and data input sheet would have been used}
To actually execute this on the LGP-21, an operator would have gone through the following sequence:
\begin{enumerate}
	\item Power on the LGP-21 and ensure the Loader Utility Program is resident in memory. Load it if not.
	\item Sit down at the Flexowriter and type out the coding sheet to produce a physical paper tape punched with the instruction bits and carriage-return stop codes (').
	\item Take the finished source tape, walk over to the LGP-21 console, and feed it into the paper tape reader.
	\item Set the console switches to execute the resident loader, and sets the input device to the paper tape reader, then tells the loader to load the tape.
	\item The loader starts the tape and reads the initial tape headers (;0004200 and /0004200), targets Track 04 Sector 00, and begins shifting the relative instruction addresses to match their new home on the drum.
	\item Once the program tape finishes loading, the operator removes it and inserts a Data Tape containing the actual floating-point numbers for $x$ and the polynomial coefficients
	\item The operator clears the manual controls and presses \textsf{START} on the physical control panel.
	\item The computer starts executing the program, which starts Interpreter 2, pauses at Line 02 (xI 0000), readss in the data values from the data tape, and immediately begins crunching the numbers through the self-modifying loop.
	\item The interpreter finishes the math, commands the Flexowriter to type out the final evaluation result on paper (\texttt{xP 0000}), drops out of interpretive mode, and triggers a hard system stop via Line 13 (\texttt{xZ 0000}).
	\item If the operator wants to clear the current run and evaluate a completely fresh data tape using the same polynomial structure, they simply hit \textsf{START} again. The machine executes Line 14 (\textsf{U 0000}), which immediately vectors execution right back to the beginning of the program loop (relative line 0) to read the next input tape of $x$ and the coefficients. 
\end{enumerate}
\newpage