\section{Trigonometric functions}

\subsection*{SINE-COSINE 1 --- B1-10.0}
\addcontentsline{toc}{subsection}{SINE-COSINE 1 --- B1-10.0}
\label{sec:sine_cosine_1}

\subsubsection*{PURPOSE}
\texttt{B1-10.0} calculates the sine or cosine of an angle value in degrees. If the angle supplied is greater than 360°, the value is reduced to an angle between 0° and 360° before the function value is calculated. A 9th-degree polynomial is used for approximation.

\texttt{B1-10.0} uses the standard LGP-21 calling sequence. The angle argument must supplied in the accumulator $q=9$. The function value, including its sign, will be in the accumulator $q=1$ when the subroutine returns.

\subsubsection*{STORAGE REQUIREMENTS}
\begin{center}
\begin{tabular}{ll}
    \toprule
    Memory & 1 track (64 words) \\
    Temporary storage & Sectors 2, 4, 5, 6, 7, 45 of track 63 \\
    \bottomrule
\end{tabular}
\end{center}

\subsubsection*{INPUT}
The input variable for \texttt{B1-10.0} is an angle in degrees, $\theta$, for which the function value is to be calculated. $\theta$ must be in the accumulator $q=9$ when the jump to the subroutine occurs. The angle is reduced to an angle between 0° and 360° by the subroutine before evaluation.

\subsubsection*{CALLING SEQUENCE}
Different calling sequences are provided for sine and cosine. Only one value (either sine or cosine) can be calculated per subroutine call.

\paragraph{Sine}
\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-1$      & \texttt{B}            & $\theta$       & Load $\theta$ at $q=9$ into the accumulator.\\
$\alpha$      & \texttt{R}            & \texttt{L+49}    & Store return address at end of subroutine.\\
$\alpha+1$      & \texttt{U}            & \texttt{L}       & Call the sine routine.\\
\bottomrule
\end{tabularx}
\end{table}


This is a standard function call, with the argument passed in the accumulator and the result returned in it.

\paragraph{Cosine}
\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-1$      & \texttt{B}            & $\theta$       & Load $\theta$ at $q=9$ into the accumulator.\\
$\alpha$      & \texttt{R}            & \texttt{L+49}    & Store return address at end of subroutine.\\
$\alpha+1$      & \texttt{U}            & \texttt{L+4}       & Call the cosine routine.\\
\bottomrule
\end{tabularx}
\end{table}

The calling sequence for cosine is exactly the same as for sine, except the subroutine is entered at the origin address of the \texttt{B1-10.0} subroutine plus 4 words.

\subsubsection*{OUTPUT}
When the subroutine returns, the properly-signed function value is in the accumulator $q=1$.

\subsubsection*{OPERATION}
\begin{itemize}
    \item \textbf{Accuracy:} The value is accurate to within 5 x $10^{-7}$.
    \item \textbf{Time required:} approximately 775 ms.
    \item \textbf{Program input:} the tape contains the program in two versions: %
    \begin{itemize}
      \item Relocatable, hexadecimal. Valid for J1-10.0 (section \ref{sec:PIRloader}).
        \item Relocatable, decimal, in coding sheet format.
    \end{itemize}
    \item \textbf{Required I/O devices:} none.
    \item \textbf{Overflow:} ignored on entry and not reset on exit.
\end{itemize}

\newpage

\subsection*{SINE-COSINE 2 --- B1-10.1}
\addcontentsline{toc}{subsection}{SINE-COSINE 2 --- B1-10.1}
\label{sec:sine_cosine_2}

\subsubsection*{PURPOSE}
\texttt{B1-10.1} calculates the sine or cosine of an angle $\theta$supplied in radians. The absolute value of the argument must not exceed $7.9999999$. Before calculating the function, the subroutine reduces the angle $\theta$ to an angle $\beta$ whose absolute value in radians is $\le$1.75. A 14th-degree Taylor series is used to calculate the function.

The program \texttt{B1-10.1} is a standard LGP-21 subroutine. The angle argument must be in the accumulator when the subroutine is called. The function value is in the accumulator $q=1$ when the function returns.

\subsubsection*{STORAGE REQUIREMENTS}
\begin{center}
\begin{tabular}{ll}
    \toprule
    Memory & 1 track (64 words) \\
    Temporary storage & Sectors 8 and 49 of track 63 \\
    \bottomrule
\end{tabular}
\end{center}

\subsubsection*{INPUT}
The input variable for \texttt{B1-10.1} is an angle $\theta$, whose function value is to be calculated. $\theta$ must be in the accumulator $q=3$ and have a value $\le 7.9999999$ when the call to the subroutine occurs. The subroutine reduces the angle to an equivalent angle $\beta$ with an absolute value <= $1.75$.

\subsubsection*{CALLING SEQUENCE}
\paragraph{Sine}
\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-1$      & \texttt{B}            & $\theta$       & Load $\theta$ at $q=3$ into the accumulator.\\
$\alpha$      & \texttt{R}            & \texttt{L+16}    & Store return address at end of subroutine.\\
$\alpha+1$      & \texttt{U}            & \texttt{L}       & Call the sine routine.\\
\bottomrule
\end{tabularx}
\end{table}

This is a standard LGP-21 function call; the argument is loaded into the accumulator before the call, and the result is returned in the accumulator.

\paragraph{Cosine}
\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-1$      & \texttt{B}            & $\theta$       & Load $\theta$ at $q=3$ into the accumulator.\\
$\alpha$      & \texttt{R}            & \texttt{L+16}    & Store return address at end of subroutine.\\
$\alpha+1$      & \texttt{U}            & \texttt{L+45}       & Call the cosine routine.\\
\bottomrule
\end{tabularx}
\end{table}

The calling sequence is exactly the same as for the sine, except that we jump to the cosine entry point to the subroutine, 45 words past the start of the subroutine.

\subsubsection*{OUTPUT}
On return, the function value with the appropriate sign is in the accumulator $q=1$.

\subsubsection*{OPERATION}
\begin{itemize}
    \item \textbf{Accuracy:} The error in radian measure is less than 8 x $10^{-9}$.
    \item \textbf{Time required:} approx. 1200 ms.
    \item \textbf{Program format:} the tape contains the program in two ways:
    \begin{itemize}
      \item Relocatable, hexadecimal, valid for J1-10.0 (section \ref{sec:PIRloader}).
        \item Relocatable, decimal, in coding sheet format.
    \end{itemize}
    \item \textbf{Required input/output devices:} None.
    \item \textbf{Overflow:} Ignored and not reset.
\end{itemize}

\newpage

\subsection*{ARCTANGENT 1 --- B1-11.0}
\addcontentsline{toc}{subsection}{ARCTANGENT 1 --- B1-11.0}
\label{sec:arctangent_1}

\subsubsection*{PURPOSE}
\texttt{B1-11.0} calculates the arctangent of any number less than 512. The returned value is signed and expressed in degrees. A 15th-degree polynomial is used to approximate the function.

\texttt{B1-11.0} is a standard LGP-21 subroutine. The argument must be in the accumulator; the arctangent value is in the accumulator on return to the main program. [Translator note: see section below on result scaling.]

This is a standard LGP-21 function call: the argument is loaded into the accumulator before the call, and the result is returned in the accumulator.

\subsubsection*{STORAGE REQUIREMENTS}
\begin{center}
\begin{tabular}{ll}
    \toprule
    Memory & 1 track (64 words) \\
    Temporary storage & Sectors 4, 5, 6, 7, 8, 9, 10, \\
     & 13, 50, and 51 of track 63 \\
    \bottomrule
\end{tabular}
\end{center}

\subsubsection*{INPUT}
The input variable for \texttt{B1-11.0} is the value $N$ at $q=9$ whose arctangent is to be computed. This value must be loaded into the accumulator before the subroutine is called.

\subsubsection*{CALLING SEQUENCE}
\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-1$      & \texttt{B}            & $N$       & Load $N$ at $q=9$ into the accumulator.\\
$\alpha$      & \texttt{R}            & \texttt{L+51}    & Store return address at end of subroutine.\\
$\alpha+1$      & \texttt{U}            & \texttt{L}       & Call the arctangent routine.\\
\bottomrule
\end{tabularx}
\end{table}

This is a standard function call. Load the argument into the accumulator before the call; the value is returned in the accumulator.

\subsubsection*{OUTPUT}
Upon returning to the main program, the signed principal value of the arctangent in degrees is in the accumulator $q=9$. The absolute value will be between 0° and 89.90°.

\subsubsection*{OPERATION}
\begin{itemize}
    \item \textbf{Accuracy:} The absolute error is no greater than 5 x $10^{-7}$ degrees.
    \item \textbf{Time required:} 950 ms.
    \item \textbf{Program format -} the tape contains the program in two ways:
    \begin{itemize}
%% fix reference
      \item Arbitrarily relocatable hexadecimal, suitable for J1-10.0 (section \ref{sec:PIRloader}).
        \item arbitrarily relocatable decimal, in coding sheet format.
    \end{itemize}
    \item \textbf{Required input/output devices:} None.
    \item \textbf{Overflow:} Neither considered at input nor reset at output.
\end{itemize}

\subsubsection*{REMARKS}
A slight modification to the subroutine call can be made to allow larger input values whose arctangents are closer to 90° (absolute) by changing the contents of memory locations $\alpha+56$ and $\alpha+59$ to rescaled values of 1 and 2.

\begin{center}
\begin{tabular}{lll}
    \toprule
    Memory location & Contents (old) & Contents (new) \\
    \midrule
    \texttt{L+56} & $1$ at $q=9$ & $1$ at $q$ \\
    \texttt{L+59} & $2$ at $q=9$ & $2$ at $q$ \\
    \bottomrule
\end{tabular}
\end{center}

The argument passed in the accumulator must be rescaled as well to the same $q$ scaling value when jumping to the subroutine.

\subsubsection*{Translator's Notes}
\paragraph{Arctangent calculations near 90 degrees}
At the default $q=9$, the input fits in roughly $\pm 2^9 \approx \pm 512$. As an angle approaches 90°, its tangent grows without bound, so an input above 512 has no valid representation and a calculation involving such a value will overflow.

To extend the range, repatch the constants at $\alpha+56$ and $\alpha+59$ to a larger $q$. The input must use the same $q$ as the constants; the routine does \emph{not} check this, and a mismatch produces silent garbage. Note also that the word width is fixed at 31 bits, so every bit you give to the integer side is taken from the fractional side --- pushing $q$ higher costs precision on smaller results.

\newpage

\subsection*{ARCTANGENT OF A VECTOR 1 --- B1-11.1}
\addcontentsline{toc}{subsection}{ARCTANGENT OF A VECTOR 1 --- B1-11.1}
\label{sec:vector_evolution_of_arctangent_1}

\subsubsection*{PURPOSE}
The program \texttt{B1-11.1} calculates $\theta$ such that, given $x$ and $y$, the following holds:
\begin{align}
    y &= \sqrt{x^2 + y^2} \sin \theta \\
    x &= \sqrt{x^2 + y^2} \cos \theta
\end{align}

The $x$ and $y$ values may be positive or negative; the function operates correctly in all quadrants of the cartesian plane. The values $x$ and $y$ must be given at the same $q$. The result is the angle expressed as a fraction of a half-turn (so 1.0 corresponds to 180° or $\pi$ radians); multiply by 180 to convert to degrees, or by $\pi$ to convert to radians.

\texttt{B1-11.1} uses a standard LGP-21 subroutine call, but the $y$ value must be stored in the subroutine's designated parameter storage, and $x$ must be in the accumulator prior to the call. Upon returning to the main program, the result will be in the accumulator $q=1$.

\subsubsection*{STORAGE REQUIREMENTS}
\begin{center}
\begin{tabular}{ll}
    \toprule
    Memory & 2 tracks, 17 sectors (145 words) \\
    Temporary storage & None \\
    \bottomrule
\end{tabular}
\end{center}

\subsubsection*{INPUT}
The input variables for the program are the values $x$ and $y$, which must have the same $q$. $y$ must be stored in the subroutine, and $x$ must be in the accumulator when the subroutine is called.

\subsubsection*{CALLING SEQUENCE}
\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-3$      & \texttt{B} & $Y$                  & Load $Y$ at $q$ into the accumulator.\\
$\alpha+2$    & \texttt{C} & \texttt{L+34} & Save into subroutine parameter storage. \\
$\alpha+1$   & \texttt{B} & \texttt{X} & Load $X$ at $q$ into the accumulator. \\
$\alpha$   & \texttt{R} & \texttt{L+10} & Set return address. \\
$\alpha+1$      & \texttt{U}          & \texttt{L}       & Call the arctangent routine.\\
\bottomrule
\end{tabularx}
\end{table}

Store $y$ in the designated parameter storage, \texttt{L+34} (any operation that puts it there is fine). Load $x$ into the accumulator (again, any operation accomplishing this is fine). Remember that $x$ and $y$ must be scaled to the same $q$ value.

\subsubsection*{OUTPUT}
On return from the subroutine, the accumulator $q=1$ will contain the result. To convert to degrees, multiply this value by 180; to convert to radians, multiply the value by $\pi$.

\subsubsection*{OPERATION}
\begin{itemize}
    \item \textbf{Accuracy:} No error exceeds $4 \times 10^{-8}$ in the half-turn result.
    \item \textbf{Time required:} approx. 1160 ms.
    \item \textbf{Program format -} the tape contains the program in two versions:
    \begin{itemize}
      \item Relocatable hexadecimal, valid for J1-10.0 (section \ref{sec:PIRloader}).
        \item Relocatable decimal, in coding sheet format.
    \end{itemize}
    \item No input/output devices are required.
    \item \textbf{Overflow:} Ignored at subroutine call, and not reset at exit.
\end{itemize}

\newpage

\subsection*{ARCSIN/ARCCOS 1 --- B1-12.0}
\addcontentsline{toc}{subsection}{ARCSIN/ARCCOS 1 --- B1-12.0}
\label{sec:arcsin_arccos_1}

\subsubsection*{PURPOSE}
The program \texttt{B1-12.0} calculates either the arcsine or the arccosine of a given number from -1 $\le N \le$ 1. The signed return value is expressed in degrees. A 7th-degree polynomial is used for approximation.

\texttt{B1-12.0} is a standard LGP-21 subroutine. When jumping to the subroutine, the argument must be in the accumulator, and the return address must be stored in the subroutine. Upon returning to the main program, the desired function value is in the accumulator $q=9$.

\subsubsection*{STORAGE REQUIREMENTS}
\begin{center}
\begin{tabular}{ll}
    \toprule
    Memory & 2 tracks, 32 sectors (160 words) \\
    Temporary storage & Sectors 12, 16, 18, 19, 20, 21, \\
     & 23, 24, 28 of Track 63 \\
    \bottomrule
\end{tabular}
\end{center}

\subsubsection*{INPUT}
The value $N$ at $q=1$, with $-1 \le N \le 1$, must be in the accumulator when the function is called.

\subsubsection*{CALLING SEQUENCE}
\paragraph{Arcsin}
\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-1$      & \texttt{B} & $N$                  & Load $N$ at $q=1$ into the accumulator.\\
$\alpha$   & \texttt{R} & \texttt{L+21} & Set return address. \\
$\alpha+1$      & \texttt{U}          & \texttt{L}       & Call the $\arcsin$ routine.\\
\bottomrule
\end{tabularx}
\end{table}

Load $N$ into the accumulator, set the return address at \texttt{L+21}, and jump to \texttt{L} to compute the $\arcsin$. Mainline execution continues after the jump.

\paragraph{Arccos}
\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-1$      & \texttt{B} & $N$                  & Load $N$ at $q=1$ into the accumulator.\\
$\alpha$   & \texttt{R} & \texttt{L+21}         & Set return address. \\
$\alpha+1$      & \texttt{U}  & \texttt{L+211}    & Call the $\arccos$ routine.\\
\bottomrule
\end{tabularx}
\end{table}

Load $N$ into the accumulator, set the return address at \texttt{L+21}, and jump to \texttt{L+211} to compute the $\arccos$. Mainline execution continues after the jump.

\subsubsection*{OUTPUT}
Output is returned in the accumulator $q=9$ in radians; the value of the function is $-\pi/2$ to $\pi/2$ for $\arcsin$, and $0$ to $\pi$ for $\arccos$.

\subsubsection*{REMARKS}
Since calculating the arcsine or arccosine requires calculating a square root, a program for this has been included in this subroutine. It can be used independently with the following calling sequence:
\begin{table}[h!]
\centering
\begin{tabularx}{\textwidth}{cccX}
\toprule
\textbf{Memory Location} & \textbf{Instruction} & \textbf{Address} & \textbf{Meaning} \\
\midrule
$\alpha-1$ & \texttt{B} & \texttt{value}  & Load \texttt{value} into the accumulator at an \emph{even} $q$ (see translator notes). \\
$\alpha$   & \texttt{R} & \texttt{L+150}  & Set return address. \\
$\alpha+1$ & \texttt{U} & \texttt{L+100}  & Call the square root routine. \\
\bottomrule
\end{tabularx}
\end{table}

The square root is returned in the accumulator. Note that the $q$ value of the result is \emph{half} of the argument's $q$ value.

\subsubsection*{Translator's Note}
\label{subsub:halfQ}
There's a lot hiding inside that throwaway ``the $q$ value of the result is half of the argument's $q$ value''. You may also be wondering why the $q$ value of the argument \emph{must} be even, especially since we're doing all this math at $q=1$ and $q=9$.

This all goes back, again, to the fractional fixed-point math of the LGP-21. In modern floating-point math, the hardware (or software) manages all the exponents for you, but on the LGP-21, something interesting happens when we take the square root of a scaled number.

If you have a value $X$ scaled to factor $q$, its internal integer representation in the machine is actually $X = x \cdot 2^q$. When you take the square root of that internal machine representation, look at what happens to the exponent:
\begin{equation}
    \sqrt{X} = \sqrt{x \cdot 2^q} = \sqrt{x} \cdot 2^{q/2}
\end{equation}
If your scale factor $q$ is an odd number (like the standard at $q=1$ or the at $q=9$ used elsewhere in these library functions), then $q/2$ is a fraction (4.5)! You cannot shift a binary word by 4.5 bits, meaning that you can't make the result of the square root operation commensurate to any $q$.

Forcing the programmer to provide an input with an even $q$ (like at $q=2$, at $q=8$, or at $q=18$) means that the resulting scale factor after the square root function finishes will be an integer (at $q=1$, at $q=4$, or at $q=9$, respectively) and that the result will be valid. 

\newpage
